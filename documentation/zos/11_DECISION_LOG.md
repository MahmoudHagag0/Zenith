# 11_DECISION_LOG

**Document ID:** ZOS-011\
**Version:** 1.0.0\
**Status:** Living Document\
**Owner:** Architecture Team

------------------------------------------------------------------------

# Purpose

The Decision Log records every approved engineering and architectural
decision made throughout the lifecycle of the Zenith project. It
provides historical traceability and links each decision to the relevant
ADR and implementation.

# Decision Entry Template

## Decision ID

DEC-YYYY-XXX

## Date

YYYY-MM-DD

## Title

Short descriptive title.

## Status

-   Proposed
-   Approved
-   Superseded
-   Deprecated

## Decision Summary

Concise description of the approved decision.

## Business Rationale

Why this decision was necessary.

## Technical Impact

Expected effects on architecture, implementation, or operations.

## Related ADR

Reference the corresponding Architecture Decision Record.

## Affected Components

List impacted applications, packages, or modules.

## Implemented In

Sprint or milestone where the decision was applied.

# Decisions

## DEC-2026-001

-   **Date:** 2026-07-11
-   **Title:** JWT-Based Authentication for S1-001 Foundation
-   **Status:** Approved
-   **Decision Summary:** S1-001's authentication foundation uses JWT-based authentication, scoped to authentication foundation, authentication middleware/base services, and protected-route capability.
-   **Business Rationale:** A confirmed mechanism was required before authentication-related implementation could begin, per Constitution Rule 1; JWT was already implied by existing naming-convention examples.
-   **Technical Impact:** `apps/api` implements JWT issuance/validation as its baseline authentication mechanism. OAuth providers, social login, and advanced identity management are explicitly out of scope and require a superseding decision.
-   **Related ADR:** ADR-001.
-   **Affected Components:** `apps/api`.
-   **Implemented In:** S1-001.

## DEC-2026-002

-   **Date:** 2026-07-11
-   **Title:** Repository Structure Conformance — `apps/api` / `apps/web`
-   **Status:** Approved
-   **Decision Summary:** The repository's placeholder `backend/` and `frontend/` directories were renamed to `apps/api` and `apps/web`, and `package.json`'s `workspaces` field updated accordingly, to conform to the already-approved structure in `13_FOLDER_STRUCTURE.md` and `01_PROJECT_OVERVIEW.md`.
-   **Business Rationale:** The live repository had diverged from previously-approved architecture (flagged during the S1-001 implementation readiness review); this decision conforms the repository to existing approved architecture rather than changing it.
-   **Technical Impact:** No architectural change — `13_FOLDER_STRUCTURE.md` already specified `apps/api`/`apps/web`. This decision authorizes bringing the repository into line with that existing specification.
-   **Related ADR:** None — this is a conformance action, not a new architectural decision; no ADR was created.
-   **Affected Components:** `apps/api`, `apps/web`, `package.json`.
-   **Implemented In:** S1-001.

## DEC-2026-003

-   **Date:** 2026-07-12
-   **Title:** Argon2id Password Hashing for S1-002 User Management
-   **Status:** Approved
-   **Decision Summary:** S1-002's `User` model stores passwords hashed with Argon2id. No other hashing algorithm is authorized.
-   **Business Rationale:** A confirmed hashing mechanism was required before any credential-handling code could be written, per Constitution Rule 1/5; no prior ZOS document specified one.
-   **Technical Impact:** `apps/api` registration/login depends on an Argon2id-capable library, to be selected and reviewed under `14_DEPENDENCY_POLICY.md` at implementation time. Password hashes must never be logged or returned in API responses (per `15_CODING_STANDARDS.md` Logging Standards).
-   **Related ADR:** ADR-002.
-   **Affected Components:** `apps/api`, `packages/database`.
-   **Implemented In:** S1-002.

## DEC-2026-004

-   **Date:** 2026-07-12
-   **Title:** Role-Gated Mutation Authorization for the S1-003 Trading Catalog
-   **Status:** Approved
-   **Decision Summary:** Trading Catalog reference data (`Exchange`, `Market`, `Asset`) is readable by any authenticated user, but creation, update, and deletion of catalog records is restricted to users with the `ADMIN` role. `Watchlist`, `WatchlistItem`, and `FavouriteAsset` records are strictly user-owned: every read and write is scoped to the requesting user's own records, and a request for another user's record returns `404 Not Found` (not `403 Forbidden`) to avoid confirming the existence of another user's resource IDs.
-   **Business Rationale:** The trading catalog is shared reference data that every trader relies on; allowing any authenticated user to mutate it would let one trader corrupt the trading universe for everyone. Restricting catalog mutation to `ADMIN` protects data integrity without requiring any new authorization architecture, because the `Role` enum (`USER`, `ADMIN`) already exists on the `User` model as of S1-002. Watchlists and favourites are personal data; ownership scoping keeps one trader's data private from another, and the 404-on-mismatch pattern prevents ID-enumeration information leaks.
-   **Technical Impact:** `apps/api` gains a `@Roles()` decorator and a `RolesGuard`, applied alongside the existing `JwtAuthGuard`, to gate catalog-mutation routes. No new authentication mechanism, no new role, and no new database field is introduced — this decision only applies the already-approved `Role` enum to new routes. Watchlist/favourite services filter every query by the authenticated user's ID and return `404` on ownership mismatch.
-   **Related ADR:** None — this decision applies the `Role` enum already approved as part of the S1-002 `User` model; it introduces no new architectural mechanism and therefore does not require a superseding or new ADR, consistent with the precedent set by DEC-2026-002.
-   **Affected Components:** `apps/api` (new `RolesGuard`, `@Roles()` decorator, catalog and watchlist/favourite modules).
-   **Implemented In:** S1-003.

## DEC-2026-005

-   **Date:** 2026-07-12
-   **Title:** Decimal Precision and Row-Level Locking for S1-004 Portfolio/Position Accounting
-   **Status:** Approved
-   **Decision Summary:** All financial quantities in the `Position` and `Transaction` models (`quantity`, `price`, `averageCost`, `realizedPnl`) are stored using Prisma's `Decimal` type (Postgres `DECIMAL`), never `Float`. Every buy/sell operation that reads and recomputes a `Position`'s running totals executes inside a database transaction that takes an explicit row lock (`SELECT ... FOR UPDATE`) on the position row before recomputing, to prevent the lost-update race that would otherwise occur if two concurrent buy/sell requests for the same position both read stale totals before writing.
-   **Business Rationale:** Floating-point arithmetic is unsuitable for cost-basis and P/L accounting — silent rounding drift would corrupt a trader's realized P/L over time, undermining the platform's credibility as a source of truth for financial decisions. Likewise, average cost and quantity are non-idempotent running totals (unlike S1-003's unique-constraint-protected entities), so concurrent buy/sell requests for the same position require explicit serialization, not just a uniqueness check, to avoid a lost update.
-   **Technical Impact:** `packages/database`'s Prisma schema uses `Decimal` columns for all financial fields; `apps/api`'s position/transaction services use `Prisma.Decimal` arithmetic (never native JS numbers) for cost-basis and P/L calculations, and wrap every buy/sell in a Prisma interactive transaction with an explicit row lock. No new dependency is introduced — `Decimal` and interactive transactions are already part of the approved Prisma/PostgreSQL stack (`04_TECH_STACK.md`).
-   **Related ADR:** None — this decision applies existing, already-approved Prisma/PostgreSQL capabilities (`Decimal` columns, interactive transactions) to a new data-integrity requirement; it introduces no new technology, mechanism, or dependency, consistent with the DEC-2026-002/DEC-2026-004 precedent.
-   **Affected Components:** `packages/database` (`Position`, `Transaction` models), `apps/api` (positions/portfolios modules).
-   **Implemented In:** S1-004.

## DEC-2026-006

-   **Date:** 2026-07-12
-   **Title:** Market Data Provider Abstraction and Simulated Default Provider
-   **Status:** Approved
-   **Decision Summary:** Records the technical implementation of ADR-003: `apps/api` sources market data exclusively through a `MarketDataProvider` interface; the only implementation registered in S1-005 is a deterministic `SimulatedMarketDataProvider`, clearly documented as not real market data.
-   **Business Rationale:** See ADR-003 — this allows the market-data foundation (caching, rate limiting, retry, background sync, API surface) to be built and verified now, without waiting on a future vendor-approval decision, while keeping every future consumer of market data completely isolated from that eventual choice.
-   **Technical Impact:** `apps/api/src/market-data/providers/` contains the interface and the simulated implementation; the module registers the simulated provider under the `MarketDataProvider` interface token, so a future provider swap requires no change outside that one registration.
-   **Related ADR:** ADR-003.
-   **Affected Components:** `apps/api/src/market-data`.
-   **Implemented In:** S1-005.

## DEC-2026-007

-   **Date:** 2026-07-12
-   **Title:** `@nestjs/schedule` for Market Data Background Synchronization
-   **Status:** Approved
-   **Decision Summary:** Records the technical implementation of ADR-004: background market-data synchronization uses `@nestjs/schedule`'s `@Cron()` decorator, added as a new runtime dependency.
-   **Business Rationale:** See ADR-004 — an official, already-maintained NestJS module is preferred over a hand-rolled interval loop or a new infrastructure dependency (e.g. Redis-backed job queue) disproportionate to this sprint's needs.
-   **Technical Impact:** `apps/api` depends on `@nestjs/schedule`; `MarketDataSyncService` runs a periodic job that refreshes cached quotes only for assets currently tracked by at least one trader (watchlisted, favourited, or held in a position), not the entire catalog, reusing the same rate limiter and retry logic as on-demand reads.
-   **Related ADR:** ADR-004.
-   **Affected Components:** `apps/api` (`package.json`, `market-data` module).
-   **Implemented In:** S1-005.

## DEC-2026-008

-   **Date:** 2026-07-12
-   **Title:** Database-Backed Market Data Caching (No New Infrastructure)
-   **Status:** Approved
-   **Decision Summary:** S1-005's local caching layer is implemented entirely in PostgreSQL via two new Prisma models — `MarketQuote` (one row per asset, refreshed on a short TTL) and `Candle` (one row per asset per trading day, cached permanently once fetched, since historical daily data does not change). No new caching infrastructure (e.g. Redis, in-memory process cache) is introduced.
-   **Business Rationale:** The existing PostgreSQL/Prisma stack is already approved and already the system of record for every other domain; reusing it for market-data caching avoids introducing new infrastructure, new operational surface, and new failure modes for a foundation sprint, consistent with `04_TECH_STACK.md`'s "keep dependencies minimal" rule.
-   **Technical Impact:** `packages/database`'s Prisma schema gains `MarketQuote` and `Candle` models. `MarketDataService` treats a quote as fresh if its `fetchedAt` is within a short TTL window, and treats a requested candle date range as fully cached only once every day in that range has a row, refetching from the provider only for genuinely missing data.
-   **Related ADR:** None — this decision applies the already-approved PostgreSQL/Prisma stack to a new caching requirement; it introduces no new technology or infrastructure, consistent with the DEC-2026-002/DEC-2026-004/DEC-2026-005 precedent.
-   **Affected Components:** `packages/database` (`MarketQuote`, `Candle` models), `apps/api` (`market-data` module).
-   **Implemented In:** S1-005.

## DEC-2026-009

-   **Date:** 2026-07-12
-   **Title:** Trading Analytics Scoring and Staleness Thresholds
-   **Status:** Approved
-   **Decision Summary:** Records the implementation-time calibration anticipated by the S1-006 Sprint Brief's Missing Decisions section: a quote is treated as stale for analytics purposes once its `fetchedAt` age exceeds 5 minutes; the Concentration Score uses a Herfindahl-Hirschman-Index-style calculation (sum of squared position weight fractions, scaled to 0-100); the Portfolio Health Score starts at 100 and subtracts bounded penalties for four named rules (position concentration, allocation balance i.e. fewer than 3 positions, missing market data, and excessive single-market-type exposure above 75%); and missing historical candle data is reported as an informational Decision Readiness factor without independently downgrading readiness, since no candle-derived metric is exposed by this sprint.
-   **Business Rationale:** These are calibration choices within an already-approved rule-based scoring design (Sprint Brief Scope items 3-5), not new architecture — analogous to choosing a cache TTL value under DEC-2026-008. Recording them here provides traceability for why a given score/status was produced, satisfying this sprint's Explainability requirement (Scope item 9) at the level of the rules themselves, not just their output.
-   **Technical Impact:** `apps/api/src/analytics/analytics.service.ts` implements these exact thresholds and formulas. Adjusting any of them (e.g. a different staleness window, different penalty weights) is a future recalibration, not a scope or architecture change, and does not require a superseding ADR.
-   **Related ADR:** None — this decision calibrates an already-approved rule-based scoring approach; it introduces no new technology or mechanism, consistent with the DEC-2026-002/DEC-2026-004/DEC-2026-005/DEC-2026-008 precedent.
-   **Affected Components:** `apps/api/src/analytics`.
-   **Implemented In:** S1-006.

## DEC-2026-010

-   **Date:** 2026-07-12
-   **Title:** Fix — Market Quote Cache Never Refreshed `fetchedAt` on Update
-   **Status:** Approved
-   **Decision Summary:** `MarketDataService.getQuote()`'s cache-freshness check compares the current time against `MarketQuote.fetchedAt`, but the S1-005 implementation's `upsert` only set `fetchedAt` on the `create` branch (relying on the column's `@default(now())`, which Prisma applies solely on INSERT). The `update` branch never included `fetchedAt`, so after an asset's very first quote, `fetchedAt` never advanced again on any subsequent refresh — meaning the cache appeared permanently stale past the initial TTL window, causing every later `getQuote()` call to make a real provider call regardless of how recently the quote was actually refreshed. This defect was dormant throughout S1-005 (whose own verification only exercised the cache within a single TTL window) and was only surfaced by S1-006 exercising the same code path over a longer time horizon for Data Quality/Confidence reporting.
-   **Decision:** `getQuote()`'s upsert now explicitly sets `fetchedAt: new Date()` on both the `create` and `update` branches.
-   **Business Rationale:** Confidence and Data Quality reporting (S1-006 Scope items 2, 6) depend on `fetchedAt` being accurate; an always-stale cache would have made every analytics response report artificially degraded confidence, and would have defeated the S1-005 quote cache's entire purpose (redundant provider calls, and unnecessary load against the rate limiter, on every single quote request).
-   **Technical Impact:** `apps/api/src/market-data/market-data.service.ts`. No schema change; no behavior change to any other field. Verified live: a quote refreshed after its TTL expired now shows an advancing `fetchedAt` on each subsequent genuine refresh, and Data Quality/Confidence correctly report `FRESH`/`HIGH` immediately after a refresh instead of `STALE`/`MEDIUM`.
-   **Related ADR:** None — this is a bug fix within already-approved architecture (DEC-2026-008's caching design), not a new decision or mechanism.
-   **Affected Components:** `apps/api/src/market-data/market-data.service.ts`.
-   **Implemented In:** S1-006 (fixing a defect introduced in S1-005).

## DEC-2026-011

-   **Date:** 2026-07-12
-   **Title:** Analysis Engine Computation Versioning, Golden-Dataset Sourcing, and Calibration Non-Defaulting
-   **Status:** Approved
-   **Decision Summary:** Records the implementation-time calibration anticipated by the S1-007 Sprint Brief's Missing Decisions section. (1) `computationVersion` uses semantic versioning starting at `1.0.0`, assigned independently per computation unit (each of the nine Indicator Engine calculators, Swing Detection, and the Regime/Context Service carries its own version, incremented only if a future change could alter a previously-computed value for the same input). (2) Wilder's original 1978 worked examples (RSI, ATR, ADX) could not be independently obtained in this implementation environment (no network access to the out-of-print primary text); per the Sprint Brief's disclosed-fallback allowance, conformance is instead verified by a fully hand-traced manual calculation over a small constructed series, with the substitution and reasoning disclosed directly in each calculator's spec file. MACD and Bollinger Bands use a hand-traced known-reference example (not a mandatory-primary-source case per the Brief's Acceptance Criteria). (3) Swing-detection `sensitivity` and Regime/Context Service threshold calibration (`adxTrendingThreshold`, `volatilityMultiplier`) are **not** given any code-level default at all — every call site must supply them explicitly, satisfying the architecture's "disclosed, never-silently-defaulted" requirement by construction rather than by choosing and recording a single default value; selecting an actual default (if one is ever wanted) is left to the S1-008 Analysis Providers that will call these services with methodology-specific values.
-   **Business Rationale:** These are calibration/documentation choices within an already-approved computation-infrastructure design (Sprint Brief Scope items 2-6), not new architecture — analogous to DEC-2026-009's scoring/staleness calibration. Recording them here provides traceability for why each conformance test uses the values it does, and confirms the deliberate absence of a swing/regime default is itself the calibration decision for this sprint, not an oversight.
-   **Technical Impact:** `apps/api/src/analysis-engine/**` — every calculator/service file sets its own `COMPUTATION_VERSION = '1.0.0'` constant; `rsi.calculator.spec.ts`, `atr.calculator.spec.ts`, `adx.calculator.spec.ts`, `macd.calculator.spec.ts`, and `bollinger-bands.calculator.spec.ts` each carry an in-file "SOURCING DISCLOSURE" / reference-example comment naming the substitution and reasoning; `SwingDetectionParams`/`RegimeContextParams` have no default values in their type definitions or call sites.
-   **Related ADR:** ADR-005 — this decision calibrates an already-approved computation-infrastructure design; it introduces no new technology or mechanism, consistent with the DEC-2026-002/DEC-2026-004/DEC-2026-005/DEC-2026-008/DEC-2026-009 precedent.
-   **Affected Components:** `apps/api/src/analysis-engine` (all calculators and services).
-   **Implemented In:** S1-007.

## DEC-2026-012

-   **Date:** 2026-07-13
-   **Title:** Analysis Provider Framework Calibration — Dependency Syntax, Tiering, Circuit Breaker, Confidence Labeling, Versioning
-   **Status:** Approved
-   **Decision Summary:** Records the implementation-time calibration anticipated by the S1-008 Sprint Brief's Missing Decisions section. (1) A Provider declares a dependency via a readonly `dependsOn?: readonly string[]` array of other Providers' stable `id`s on the `AnalysisProvider` interface itself — resolved by the Execution Engine via topological sort, never a concrete-class import. (2) Tier classification is a readonly `tier: 'FAST' | 'SLOW'` field, also self-declared on the Provider. (3) The circuit breaker uses a `failureThreshold` of 3 consecutive failures/timeouts and a `resetTimeoutMs` of 30,000ms before allowing a probe retry; the default per-invocation timeout (overridable per Provider via `timeoutMs`) is 5,000ms. (4) The four Confidence-taxonomy labels are the `ConfidenceKind` union (`'DETECTION' | 'INTERPRETATION' | 'REGIME_ADJUSTED' | 'METHODOLOGY_CEILING'`), each paired with a `Prisma.Decimal` value on a 0-100 scale (consistent with the project's existing 0-100 scored fields, e.g. S1-006's Concentration/Health Score) rather than a categorical HIGH/MEDIUM/LOW label, since `InterpretationConfidence` must support ranking multiple hypotheses within a single `interpretation[]` array — a categorical scale would not. (5) `computationVersion` follows the same semantic-versioning-from-`1.0.0` convention as S1-007 (DEC-2026-011), assigned per Provider. (6) `runNewAnalysis()` returns two independently-resolvable results (`fastTier`/`slowTier` promises, an implementation-level `TieredExecutionRun` shape) rather than one flat awaited value, so a SLOW-tier Provider's execution can never block a FAST-tier Provider's result, per ADR-006's Execution tiers requirement — each Provider's own invocation only awaits its actual declared dependencies (a per-node async task-graph), not an artificial tier-wide barrier.
-   **Business Rationale:** These are calibration/type-shape choices within an already-approved framework design (ADR-006; Sprint Brief Scope items 1-6), not new architecture — analogous to DEC-2026-009's and DEC-2026-011's precedent. No real Provider exists yet to be affected by a future recalibration of the numeric thresholds; S1-009's first real Provider inherits these values unless a superseding Decision Log entry changes them.
-   **Technical Impact:** `apps/api/src/analysis-engine/providers/**`. `provider-circuit-breaker.ts` and `provider-execution.service.ts` hold the threshold/timeout constants as named, commented constants (not magic numbers). `analysis-provider.types.ts` defines `ConfidenceKind`/`LabeledConfidence`/`ProviderTier`/`ProviderLifecycleState`. `provider-execution.types.ts` defines `TieredExecutionRun`. A sprint-audit fix (not a new decision) also scoped `ExecutionRunResult.totalRegistered` to the tier actually being reported, rather than the grand total across both tiers.
-   **Related ADR:** ADR-006 — this decision calibrates an already-approved framework design; it introduces no new technology or mechanism, consistent with the DEC-2026-002/DEC-2026-004/DEC-2026-005/DEC-2026-008/DEC-2026-009/DEC-2026-011 precedent.
-   **Affected Components:** `apps/api/src/analysis-engine/providers`.
-   **Implemented In:** S1-008.

## DEC-2026-013

-   **Date:** 2026-07-13
-   **Title:** Wyckoff Method Provider Calibration, Golden-Dataset Sourcing, and Generic Confidence Contract Completion
-   **Status:** Approved
-   **Decision Summary:** Records the implementation-time calibration anticipated by the S1-009 Sprint Brief's Missing Decisions section, plus two framework-level fixes discovered while building the first real Provider. (1) The maximum number of alternate phase-hypotheses tracked is 2 (`MAX_PHASE_HYPOTHESES`), returned only at the two genuinely ambiguous schematic transitions (post-AR; post-Spring/Upthrust pre-Test). (2) Event-detection thresholds: a Selling/Buying Climax requires volume `>=2x` (`CLIMAX_VOLUME_MULTIPLIER`) the trailing 5-bar average (`TRAILING_VOLUME_WINDOW`); a Secondary Test/Test's "near price" tolerance is `1.5x` the current ATR (`NEAR_TOLERANCE_ATR_MULTIPLIER`), not a fixed percentage, so it scales with each instrument's own volatility. (3) `WyckoffProvider`'s own calibration for the shared computation calls it makes: swing sensitivity `3`, ADX/ATR periods `14`, ADX trending threshold `25`, volatility multiplier `1.5`. (4) Golden-dataset sourcing: the Wyckoff Stock Market Institute course material's specific page-numbered worked example could not be independently obtained in this implementation environment; the golden-dataset test instead reproduces the canonical, universally-taught Accumulation Schematic #1 structure and qualitative price/volume relationships every published description agrees on, disclosed in-file per the S1-007 precedent. (5) Methodology Confidence Ceiling for `'WYCKOFF'` is `85`, reflecting its disclosed source-verified status (Wyckoff's Three Laws) relative to methodologies lacking an independent primary source. (6) `computationVersion`/`contractVersion` both `1.0.0`, the same convention as S1-007/S1-008. **Two framework-level fixes, not Wyckoff-specific:** the generic `AnalysisProviderResult`/`Interpretation` contract (S1-008) only ever carried Interpretation Confidence — completed to also carry `detectionConfidence` and `methodologyConfidenceCeiling` (one per result) and `regimeAdjustedConfidence` (per interpretation entry), since ADR-006 requires all four Confidence kinds to be expressible on every Provider's output, not just one. Separately, `ANALYSIS_PROVIDERS`'s factory registration moved from a standalone `ProvidersModule` into `AnalysisEngineModule` directly, because NestJS module encapsulation does not allow a module to inject a provider declared only in a module that imports it — the factory needs `INDICATOR_ENGINE`/`SWING_DETECTOR`/`REGIME_CONTEXT`, which live in `AnalysisEngineModule`.
-   **Business Rationale:** The numeric thresholds and sourcing disclosure are calibration choices within an already-approved Provider design (S1-009 Sprint Brief Scope items 1-10), not new architecture — analogous to the DEC-2026-009/011/012 precedent. The two framework-level fixes are completions of already-approved, already-mandated generic requirements (ADR-006's four-part Confidence taxonomy; NestJS's own module-scoping rules), discovered only once a real Provider first exercised them — the same category as S1-006's DEC-2026-010 (a defect found by the first sprint to genuinely exercise a prior sprint's code path), not a reversal of any prior decision.
-   **Technical Impact:** `apps/api/src/analysis-engine/providers/wyckoff/**` holds every disclosed constant as a named, commented value. `apps/api/src/analysis-engine/providers/analysis-provider.types.ts` gained `AnalysisProviderResult.detectionConfidence`/`.methodologyConfidenceCeiling` and `Interpretation.regimeAdjustedConfidence` (additive; the S1-008 `FixtureProvider` and its tests were updated to match, with zero behavioral change to dependency resolution, tiering, circuit breaker, or lifecycle gating). `apps/api/src/analysis-engine/analysis-engine.module.ts` now registers `ANALYSIS_PROVIDERS`/`PROVIDER_EXECUTION_ENGINE` directly; `providers/providers.module.ts` and its spec were removed (nothing else referenced them).
-   **Related ADR:** ADR-005 (computation substrate calibration precedent), ADR-006 (Provider Confidence taxonomy and module-registration mechanics) — this decision calibrates and completes already-approved designs; it introduces no new technology or mechanism, consistent with the DEC-2026-009/011/012 precedent.
-   **Affected Components:** `apps/api/src/analysis-engine/providers/wyckoff`, `apps/api/src/analysis-engine/providers/analysis-provider.types.ts`, `apps/api/src/analysis-engine/analysis-engine.module.ts`.
-   **Implemented In:** S1-009.

## DEC-2026-014

-   **Date:** 2026-07-13
-   **Title:** ICT/SMC Provider Calibration, Golden-Dataset Sourcing, and the DisplacementLeg Internal Concept
-   **Status:** Approved
-   **Decision Summary:** Records the implementation-time calibration anticipated by the S1-010 Sprint Brief's Missing Decisions section, plus the disclosed internal design introduced under the Architecture Team's Implementation Guidance #1. (1) The maximum number of alternate bias-hypotheses tracked is 2 (`MAX_BIAS_HYPOTHESES`), returned only when Bullish and Bearish evidence totals genuinely tie. (2) Event-detection thresholds: a Fair Value Gap's minimum qualifying size is `0.25x` the current ATR (`FVG_MIN_ATR_MULTIPLIER`); a Liquidity Sweep's pierce-beyond-tolerance is also `0.25x` ATR (`SWEEP_ATR_TOLERANCE_MULTIPLIER`); an Order Block's backward search window is 5 bars (`ORDER_BLOCK_LOOKBACK_BARS`); a Liquidity Sweep's best-effort link to a following Displacement Leg looks up to 3 bars ahead (`SWEEP_TO_DISPLACEMENT_WINDOW_BARS`) — all disclosed, named constants, none a fixed price-fraction. (3) This Provider's own calibration for the shared computation calls it makes: swing sensitivity `3` (matching the value chosen for S1-009's Provider for cross-Provider structural consistency, not because of any dependency between them), ADX/ATR periods `14`, ADX trending threshold `25`, volatility multiplier `1.5`. (4) Golden-dataset sourcing: no single canonical, page-numbered ICT/SMC worked example could be independently obtained in this implementation environment (unlike a single institutional curriculum, ICT/SMC vocabulary is taught across a large, decentralized body of modern retail trading education with real definitional variance between sources); the golden-dataset test instead reproduces the canonical, universally-taught "liquidity sweep then displacement" setup every mainstream source agrees on, with this Provider's exact Order Block/Fair Value Gap/Liquidity Sweep definitions disclosed in the same file, per the S1-007/S1-009 precedent. (5) Methodology Confidence Ceiling for `'ICT_SMC'` is `60`, strictly below Wyckoff's `85`, reflecting this methodology's disclosed absence of independent institutional verification. (6) `computationVersion`/`contractVersion` both `1.0.0`, the same convention as S1-007/S1-008/S1-009. (7) Regime-Adjusted Confidence uses a mirror-image rule to Wyckoff's: an Order-Block-dominant (continuation) reading is strengthened (`x1.2`) in `TRENDING` and weakened (`x0.7`) in `RANGING`; a Liquidity-Sweep-dominant (reversal) reading is strengthened in `RANGING` and weakened in `TRENDING`; a tied reading is not scaled. **One disclosed internal design decision, per the Architecture Team's binding Implementation Guidance #1 (S1-010 Sprint Brief, Approval Section):** `DisplacementLeg` — the impulse leg following a Swing Detector `BOS` event — is introduced as a shared, ICT-internal concept linking Order Block (`INSTITUTIONAL_REACTION`), Fair Value Gap (`IMBALANCE`), and Liquidity Sweep (`LIQUIDITY_EVENT`) detection, expressing the `Liquidity Event -> Displacement -> Imbalance -> Institutional Reaction` narrative structurally without implementing its full reasoning in V1. Per the Architecture Team's explicit direction at Task Breakdown approval, this concept remains confined to `providers/ict-smc/` and is not promoted into the generic Analysis Provider Framework; a future Provider demonstrating genuine reuse would require its own ADR to do so.
-   **Business Rationale:** The numeric thresholds, sourcing disclosure, and Confidence Ceiling are calibration choices within an already-approved Provider design (S1-010 Sprint Brief Scope items 1-11), not new architecture — analogous to the DEC-2026-009/011/012/013 precedent. The `DisplacementLeg` concept is a Provider-internal design choice explicitly reviewed and approved by the Architecture Team at Task Breakdown approval, not a unilateral framework change — it is confined by a mechanical boundary test (alongside the independence boundary test) to never leak into any generic component.
-   **Technical Impact:** `apps/api/src/analysis-engine/providers/ict-smc/**` holds every disclosed constant as a named, commented value; `ict-smc.types.ts` defines `DisplacementLeg`/`OrderBlock`/`FairValueGap`/`LiquiditySweep`/`IctSmcBiasHypothesis`, all confined to this directory. `apps/api/src/analysis-engine/analysis-engine.module.ts` registers `IctSmcProvider` as the second `ANALYSIS_PROVIDERS` entry, constructed with the same shared `INDICATOR_ENGINE`/`SWING_DETECTOR`/`REGIME_CONTEXT` instances as `WyckoffProvider`, with no dependency between the two Providers. No change to the generic `AnalysisProvider`/`AnalysisProviderResult` contract, the Execution Engine, Lifecycle, or Observability — this Provider is a pure consumer of the S1-008 framework, exactly as ADR-006 requires.
-   **Related ADR:** ADR-005 (computation substrate calibration precedent), ADR-006 (Provider Confidence taxonomy and registration mechanics) — this decision calibrates an already-approved design; it introduces no new technology or mechanism, consistent with the DEC-2026-009/011/012/013 precedent.
-   **Affected Components:** `apps/api/src/analysis-engine/providers/ict-smc`, `apps/api/src/analysis-engine/analysis-engine.module.ts`.
-   **Implemented In:** S1-010.

## DEC-2026-015

-   **Date:** 2026-07-13
-   **Title:** Elliott Wave Provider Calibration, Golden-Dataset Sourcing, and Fibonacci-Guideline Reuse
-   **Status:** Approved
-   **Decision Summary:** Records the implementation-time calibration anticipated by the S1-011 Sprint Brief's Missing Decisions section. (1) The maximum number of alternate wave-count hypotheses tracked is 2 (`MAX_WAVE_COUNT_HYPOTHESES`), returned only when the ranking (Fibonacci-guideline proximity) admits more than one comparably-scored survivor; candidate generation itself is bounded by construction (a linear scan over consecutive Swing Detector windows, never a combinatorial subset search), not by this cap. (2) Fibonacci-guideline proximity uses a `0.05x` (5% of the reference wave's own range) tolerance (`GUIDELINE_TOLERANCE_RATIO`), proportional rather than ATR-relative, since a retracement/extension guideline is inherently proportional to the wave being measured. (3) A Rule margin or guideline-proximity score below `40` (`WEAKNESS_THRESHOLD`) is disclosed as a weakness (Implementation Guidance #5). (4) This Provider's own calibration for the shared computation calls it makes: swing sensitivity `3` (matching both prior Providers' value, for cross-Provider structural consistency, not dependency), ADX/ATR periods `14`, ADX trending threshold `25`, volatility multiplier `1.5`. (5) Golden-dataset sourcing: R.N. Elliott's own 1938 "The Wave Principle" could not be independently obtained in this implementation environment; the golden-dataset test instead reproduces the canonical, idealized 5-wave impulse (61.8% Wave 2 retracement, 1.618x Wave 3 extension, 38.2% Wave 4 retracement) every mainstream secondary source (most notably Frost & Prechter's "Elliott Wave Principle," 1978) agrees on, per the S1-007/S1-009/S1-010 precedent. (6) Methodology Confidence Ceiling for `'ELLIOTT_WAVE'` is `75` — independently calibrated between Wyckoff's `85` and ICT/SMC's `60`, reflecting a genuine but decades-old, less-immediately-accessible primary source and a methodology whose real-world application is widely documented to admit multiple simultaneously-defensible readings. (7) Regime-Adjusted Confidence strengthens (`x1.2`) in `TRENDING` and weakens (`x0.7`) in `RANGING` — a third distinct pattern from both prior Providers' own rules, reflecting that an impulse-wave count is fundamentally a trending-market structure. (8) `computationVersion`/`contractVersion` both `1.0.0`, the same convention as every prior Provider. **One disclosed design decision, per the Architecture Team's binding Implementation Guidance #5/#6 (S1-011 Sprint Brief, Approval Section):** Detection Confidence is computed as the *minimum* (not average) of a candidate's three Rule margins — the weakest link determines overall structural confidence — and every surviving hypothesis's disclosed summary text states, in explicit and separate parts, why it survives, what weakens it, and what would invalidate it, worded throughout as "the strongest currently-surviving interpretation," never as an objectively correct count. **A genuine, non-obvious finding surfaced during golden-dataset testing (WP11):** a textbook-perfect Fibonacci-guideline match (high Interpretation Confidence) does not imply a wide Rule-safety margin (Detection Confidence) — these are correctly independent dimensions of confidence, not a defect; a Wave 4 retracing a "clean" 38.2% of Wave 3 can still leave only a moderate margin against Rule 3, and the Provider discloses this honestly rather than conflating the two.
-   **Business Rationale:** The numeric thresholds, sourcing disclosure, and Confidence Ceiling are calibration choices within an already-approved Provider design (S1-011 Sprint Brief Scope items 1-11), not new architecture — analogous to the DEC-2026-009/011/012/013/014 precedent. The Detection Confidence design and the three-part hypothesis disclosure are direct implementations of the Architecture Team's own binding guidance, not unilateral additions.
-   **Technical Impact:** `apps/api/src/analysis-engine/providers/elliott-wave/**` holds every disclosed constant as a named, commented value; `elliott-wave.types.ts` defines `ImpulseWaveLeg`/`WaveInvalidation`/`RuleMargins`/`FibonacciGuidelineCheck`/`WaveCountCandidate`, all confined to this directory. `apps/api/src/analysis-engine/analysis-engine.module.ts` registers `ElliottWaveProvider` as the third `ANALYSIS_PROVIDERS` entry, constructed with the same shared `INDICATOR_ENGINE`/`SWING_DETECTOR`/`REGIME_CONTEXT` instances as the two prior Providers, with no dependency on either. No change to the generic `AnalysisProvider`/`AnalysisProviderResult` contract, the Execution Engine, Lifecycle, or Observability — this Provider is a pure consumer of the S1-008 framework. `INDICATOR_ENGINE.fibonacciLevels()` (S1-007) is consumed for the first time since its creation, unmodified.
-   **Related ADR:** ADR-005 (computation substrate calibration precedent), ADR-006 (Provider Confidence taxonomy and registration mechanics) — this decision calibrates an already-approved design; it introduces no new technology or mechanism, consistent with the DEC-2026-009/011/012/013/014 precedent.
-   **Affected Components:** `apps/api/src/analysis-engine/providers/elliott-wave`, `apps/api/src/analysis-engine/analysis-engine.module.ts`.
-   **Implemented In:** S1-011.

## DEC-2026-016

-   **Date:** 2026-07-13
-   **Title:** Confluence Engine Vocabulary Mapping, Strength Calibration, and Attribution Bound
-   **Status:** Approved
-   **Decision Summary:** Records the implementation-time calibration anticipated by the S1-012 Sprint Brief's Missing Decisions section. (1) Per-Provider `normalize()` dimension mappings, confined to each Provider's own module directory (ADR-007, "decentralized by implementation"): **Wyckoff** — TREND from Accumulation-side events (Selling Climax/Sign of Strength/Last Point of Support → BULLISH) vs. Distribution-side events (Buying Climax/Sign of Weakness/Last Point of Supply → BEARISH); STRUCTURE only from Sign of Strength/Weakness; VOLUME only from Selling/Buying Climax (this methodology's own native volume-spike criterion); CONFIRMATION only from Last Point of Support/Supply; MOMENTUM/LIQUIDITY/VOLATILITY always `NOT_APPLICABLE` (no native concept in V1 scope); `strength` = `detectionConfidence`. **ICT/SMC** — TREND from the primary bias hypothesis's own "Bullish bias"/"Bearish bias" summary text; LIQUIDITY from a detected Liquidity Sweep's own direction; STRUCTURE from a detected Order Block's own direction; CONFIRMATION from a detected Fair Value Gap's own direction; MOMENTUM/VOLATILITY/VOLUME always `NOT_APPLICABLE` (this methodology never uses volume at all, per S1-010's own disclosed design); `strength` = `detectionConfidence`. **Elliott Wave** — TREND and STRUCTURE both directly from the primary wave count's own direction word (they necessarily always agree with each other for this Provider, since both derive from the same underlying wave count); MOMENTUM matches TREND's direction only when Interpretation Confidence (Fibonacci-guideline proximity) is `>= 60` (`MOMENTUM_CONFIDENCE_THRESHOLD`), else `NOT_APPLICABLE`; CONFIRMATION matches TREND's direction only when Detection Confidence (weakest Rule margin) is `>= 50` (`CONFIRMATION_MARGIN_THRESHOLD`), else `NOT_APPLICABLE`; LIQUIDITY/VOLATILITY/VOLUME always `NOT_APPLICABLE`. Across all three Providers, whenever a result's `interpretation[]` is empty (the Limitations/graceful-degradation path — nothing was evaluated at all), every one of the seven dimensions reads `NOT_APPLICABLE`, never the ambiguous-default `NEUTRAL` — "nothing evaluated" is a genuinely different state from "evaluated and found balanced," and conflating them would misrepresent a Provider's own silence as a considered, neutral opinion. (2) `strength` (0–100) is calibrated identically across all three Providers as that Provider's own `detectionConfidence.value` (or, for Elliott Wave's MOMENTUM/CONFIRMATION signals specifically, still `detectionConfidence`, gated by the separate Interpretation-Confidence/Detection-Confidence thresholds above) — reusing each Provider's own already-disclosed, already-calibrated Confidence taxonomy rather than inventing a second, parallel scale. (3) The bounded top-N contributing-Provider attribution per disagreement side is `3` (`MAX_CONTRIBUTORS_PER_SIDE`, Finding C's own recommendation, `22_ANALYSIS_ENGINE_ARCHITECTURE.md`), selected by each vote's own `strength` (post-family-aggregation), preserving the `O(Providers × 7)` bound regardless of how many Providers register in the future. (4) `NormalizedProviderOutput.vocabularySchemaVersion` starts at `'1.0.0'`, the same convention as every prior sprint's `computationVersion`/`contractVersion`. **One additional, non-Missing-Decision finding surfaced and corrected during this Sprint's own Sprint Audit (WP14):** `aggregateDimension`'s call to `ConfluenceWeightStrategy.computeWeight()` was passing a hardcoded `undefined` for `methodologyFamily` instead of each vote's own genuine family, invisible today only because `EqualWeightStrategy` ignores both of its parameters — corrected so `FamilyVote` now carries its representative's own `methodologyFamily` through to the weight-strategy call, with a dedicated regression test (`confluence-dimension-aggregator.util.spec.ts`) proving the strategy receives the true value, not a silent default, before any future differential (family-aware) weighting strategy is built on top of it.
-   **Business Rationale:** The per-Provider dimension mappings, strength calibration, and attribution bound are calibration choices within an already-approved design (S1-012 Sprint Brief Scope items 1-10; ADR-007), not new architecture — analogous to the DEC-2026-009/011/012/013/014/015 precedent. The `methodologyFamily`-propagation fix is a correctness repair to an already-approved mechanism (Scope item 6), not a new decision, disclosed here for traceability per this Sprint's own quality discipline (self-review after every Work Package, Sprint Audit before closure).
-   **Technical Impact:** `apps/api/src/analysis-engine/providers/normalized-vocabulary.types.ts` (new, shared schema); `analysis-provider.types.ts` (`normalize()`'s signature completed); `providers/wyckoff/wyckoff-normalize.util.ts`, `providers/ict-smc/ict-smc-normalize.util.ts`, `providers/elliott-wave/elliott-wave-normalize.util.ts` (new, one per Provider, each confined to its own directory); `providers/normalize-vocabulary-conformance.spec.ts` (new, shared conformance suite); `apps/api/src/analysis-engine/confluence/**` (new module: `confluence.types.ts`, `confluence.tokens.ts`, `equal-weight.strategy.ts`, `confluence-family-grouping.util.ts`, `confluence-dimension-aggregator.util.ts`, `confluence.service.ts`, plus a golden-dataset conformance test reusing Wyckoff's (S1-009 WP10) and ICT/SMC's (S1-010 WP11) own already-proven golden-dataset fixtures); `analysis-engine.module.ts` registers `CONFLUENCE_WEIGHT_STRATEGY`/`CONFLUENCE_ENGINE`. No change to the Execution Engine, Provider Lifecycle, Registry, or any Provider's own detection/Confidence/Traceability logic beyond the one `normalize()` method each. No new Prisma model, no new HTTP surface, no new runtime dependency.
-   **Related ADR:** ADR-006 (`normalize()`'s signature completion, explicitly anticipated), ADR-007 (Normalized Vocabulary, `ConfluenceWeightStrategy`, family-aware grouping, dimension-level disagreement, participation reporting, traceability-by-reference — this decision calibrates, not redesigns, ADR-007's already-approved content).
-   **Affected Components:** `apps/api/src/analysis-engine/providers` (normalize() completion + per-Provider mappings), `apps/api/src/analysis-engine/confluence` (new), `apps/api/src/analysis-engine/analysis-engine.module.ts`.
-   **Implemented In:** S1-012.

## DEC-2026-017

-   **Date:** 2026-07-13
-   **Title:** Harmonic Patterns Provider Ratio-Table Calibration, Golden-Dataset Sourcing, and Confidence Design
-   **Status:** Approved
-   **Decision Summary:** Records the implementation-time calibration anticipated by the S1-013 Sprint Brief's Missing Decisions section. (1) Ratio tolerance bands for the four V1 patterns, each with a disclosed cited-ideal value: **Gartley** — `AB` `[0.58, 0.66]` (ideal `0.618`), `BC` `[0.382, 0.886]` (ideal `0.618`), `CD` `[1.13, 1.618]` (ideal `1.272`), `AD` `[0.75, 0.82]` (ideal `0.786`); **Bat** — `AB` `[0.382, 0.5]` (ideal `0.446`), `BC` `[0.382, 0.886]` (ideal `0.618`), `CD` `[1.618, 2.618]` (ideal `2.0`), `AD` `[0.856, 0.9]` (ideal `0.886`); **Butterfly** — `AB` `[0.75, 0.82]` (ideal `0.786`), `BC` `[0.382, 0.886]` (ideal `0.618`), `CD` `[1.618, 2.24]` (ideal `1.618`), `AD` `[1.27, 1.618]` (ideal `1.27`); **Crab** — `AB` `[0.382, 0.618]` (ideal `0.5`), `BC` `[0.382, 0.886]` (ideal `0.618`), `CD` `[2.24, 3.618]` (ideal `3.14`), `AD` `[1.58, 1.66]` (ideal `1.618`) — reproducing the widely-taught canonical ratio table found consistently across independent secondary sources, per the disclosed-fallback allowance (Gartley's 1935 original, Pesavento's 1978, and Carney's 2004 texts could not be independently obtained in this environment). **A genuine, non-obvious structural finding surfaced during WP3 self-review:** with these honestly-sourced bands, the four patterns' own `AD` ratio bands are mutually disjoint (each pattern's own primary differentiator, consistent with how the methodology is taught) — meaning true simultaneous multi-pattern ambiguity essentially cannot occur through realistic price action with today's four patterns. `matchPatternTypes()` therefore exposes an optional `tables` parameter (defaulting to the real, disclosed table set) so the "check every table independently, never merge" mechanism itself is proven correct via a synthetic overlapping-bands fixture, independent of whether today's specific tables happen to exercise it — the mechanism, not today's incidental data, is what Scope item 5 requires be correct. (2) The bounded maximum for `interpretation[]` is `2` (`MAX_PATTERN_HYPOTHESES`), the same bound as Elliott Wave's own wave-count hypotheses. (3) Interpretation Confidence weights ideal-ratio proximity and AB=CD time-symmetry equally (50/50 average) — two genuinely distinct signals (band-edge margin vs. cited-ideal-value proximity; and a secondary, widely-cited harmonic confirmation independent of price-ratio conformance), neither is treated as more authoritative than the other absent evidence to prefer one. (4) Regime-Adjusted Confidence multiplies Interpretation Confidence by `1.2` in `LOW` volatility and `0.7` in `HIGH` volatility — using `RegimeContextResult.volatilityState`, not `trendState`, since a harmonic pattern's own claim rests on precise ratio geometry, more trustworthy when price action is clean. (5) Golden-dataset sourcing: none of Gartley's 1935 original text, Pesavento's 1978 ratio work, or Carney's 2004 "Harmonic Trading" could be independently obtained in this implementation environment; the golden-dataset test instead reproduces the canonical bullish Gartley instance (B at `0.618` XA retracement, D at `0.786` XA retracement) every independent secondary source on this methodology agrees on. (6) Methodology Confidence Ceiling for `'HARMONIC_PATTERNS'` is `65` — independently calibrated, reflecting genuine ratio-table variance across this methodology's own competing authors, a decentralized-authorship risk distinct from every other registered methodology's own sourcing profile. (7) `computationVersion`/`vocabularySchemaVersion` both `1.0.0`, the same convention as every prior Provider. (8) This Provider deliberately does not inject or call `INDICATOR_ENGINE.fibonacciLevels()` — each of its four ratios has its own distinct, sequentially-chained anchor-leg pair, making a direct `Prisma.Decimal` ratio computation both simpler and more directly traceable than forcing an anchor-pair-based calculator into a shape it was not designed for; a disclosed engineering choice (Sprint Brief Architecture Requirements), not an oversight or an inconsistency with the reuse precedent set elsewhere in this system.
-   **Business Rationale:** The ratio-band calibration, sourcing disclosure, Confidence design, and Methodology Confidence Ceiling are calibration choices within an already-approved Provider design (S1-013 Sprint Brief Scope items 1-15), not new architecture — analogous to the DEC-2026-009/011/012/013/014/015/016 precedent. The `tables`-parameter testing seam is a minimal, disclosed, purely-additive mechanism-verification choice, not a new production code path or a promotion of any concept into the generic framework.
-   **Technical Impact:** `apps/api/src/analysis-engine/providers/harmonic-patterns/**` holds every disclosed ratio table and constant as named, commented values; `harmonic-patterns.types.ts` defines `PatternLeg`/`PatternRatioTable`/`RatioBand`/`LegRatioCheck`/`HarmonicPatternCandidate`/`PatternInvalidation`, all confined to this directory. `apps/api/src/analysis-engine/analysis-engine.module.ts` registers `HarmonicPatternsProvider` as the fourth `ANALYSIS_PROVIDERS` entry, constructed with the same shared `SWING_DETECTOR`/`REGIME_CONTEXT` instances as the three prior Providers (deliberately not `INDICATOR_ENGINE`), with no dependency on any of them. `normalize()` added as a fourth fixture to the existing shared conformance suite (`normalize-vocabulary-conformance.spec.ts`, S1-012) — no change to that suite's own generic assertion logic. No change to the generic `AnalysisProvider`/`AnalysisProviderResult` contract, the Execution Engine, Lifecycle, Confluence Engine, or Normalized Vocabulary Schema — this Provider is a pure consumer of the S1-008/S1-012 frameworks.
-   **Related ADR:** ADR-006 (Provider contract mechanics), ADR-007 (Normalized Vocabulary, consumed via a new `normalize()` mapping) — this decision calibrates an already-approved design; it introduces no new technology, mechanism, or vocabulary dimension, consistent with the DEC-2026-009/011/012/013/014/015/016 precedent.
-   **Affected Components:** `apps/api/src/analysis-engine/providers/harmonic-patterns`, `apps/api/src/analysis-engine/providers/normalize-vocabulary-conformance.spec.ts`, `apps/api/src/analysis-engine/analysis-engine.module.ts`.
-   **Implemented In:** S1-013.

## DEC-2026-018

-   **Date:** 2026-07-13
-   **Title:** Classical Chart Patterns Provider Shape-Tolerance Calibration, Confirmation Design, and Golden-Dataset Sourcing
-   **Status:** Approved
-   **Decision Summary:** Records the implementation-time calibration anticipated by the S1-014 Sprint Brief's Missing Decisions section. (1) `SYMMETRY_TOLERANCE_RATIO = 0.1` (10% of the two prices' own average) governs both Head and Shoulders' shoulder/neckline-levelness checks and Double Top/Bottom's peak/trough-equality check — a single shared tolerance across both pattern families, reproducing the widely-taught "roughly equal" qualitative description every independent secondary source on this methodology agrees on (Edwards & Magee's own 1948 primary text could not be independently obtained in this environment, per the disclosed-fallback allowance). (2) `VOLUME_EXPANSION_MARGIN_RATIO = 0.2` (the confirming close's own volume must exceed the pattern's formation-period average volume by 20%) upgrades a `CONFIRMED` reading to `VOLUME_CONFIRMED` — Edwards & Magee's own emphasis that a genuine breakout is accompanied by expanding volume. (3) Interpretation Confidence base values per confirmation state: `UNCONFIRMED = 35`, `CONFIRMED = 65`, `VOLUME_CONFIRMED = 90` — a genuinely novel design for this methodology's own temporal confirmation concept, distinct in kind from Elliott Wave's Rule-margin/guideline-proximity split and Harmonic Patterns' band-margin/ideal-proximity split (neither of which has an equivalent "has price since done something" signal). (4) The bounded maximum for `interpretation[]` is `2` (`MAX_CHART_PATTERN_HYPOTHESES`), the same bound as every prior bounded-hypothesis Provider. (5) Regime-Adjusted Confidence multiplies Interpretation Confidence by `1.15` in `TRENDING` and `0.75` in `RANGING` — independently calibrated (not copied from Elliott Wave's `1.2`/`0.7`), reflecting this Provider's own reasoning: a reversal pattern's claim requires a genuine prior trend to reverse. (6) Golden-dataset sourcing: Edwards & Magee's 1948 "Technical Analysis of Stock Trends" could not be independently obtained in this implementation environment; the golden-dataset test instead reproduces the canonical Head and Shoulders (Top) instance — a Head exceeding two roughly-symmetric Shoulders, a roughly level neckline, and a volume-expanding confirming break — every independent secondary source on this methodology agrees on. (7) Methodology Confidence Ceiling for `'CLASSICAL_CHART_PATTERNS'` is `80` — independently calibrated, reflecting Edwards & Magee's status as an exceptionally well-documented, continuously reprinted, single-lineage primary source, distinct from every other registered methodology's own ceiling (`60`, `65`, `75`, `85`). (8) `computationVersion`/`vocabularySchemaVersion` both `1.0.0`, the same convention as every prior Provider. **One genuine, non-obvious structural finding surfaced during WP1-WP12 self-review, not a defect:** a Head and Shoulders candidate's own neckline (its two Troughs) is, by the shape criteria's own definition, roughly level — which structurally also satisfies a Double-Bottom-shape check on those same three points (neckline-levelness and trough-symmetry are the same computation). Genuine structural overlap between the two pattern families is therefore common, not rare (the mirror-image finding to Harmonic Patterns' own S1-013 discovery that its four patterns' `AD` bands are mutually disjoint, making overlap rare there) — both are honestly disclosed as independent, never-merged hypotheses per this Sprint's own bounded multi-hypothesis design (Scope item 7), and the golden-dataset/provider test assertions were written to search for the specific pattern-type entry under test rather than assume single-hypothesis output, once this was understood.
-   **Business Rationale:** The shape-tolerance calibration, confirmation-scoring design, sourcing disclosure, and Methodology Confidence Ceiling are calibration choices within an already-approved Provider design (S1-014 Sprint Brief Scope items 1-13), not new architecture — analogous to the DEC-2026-009/011/012/013/014/015/016/017 precedent. The structural-overlap finding is a correct characterization of the methodology's own geometry, not a defect requiring correction; the bounded multi-hypothesis mechanism (S1-012/S1-013 precedent) already exists precisely to represent this kind of genuine ambiguity.
-   **Technical Impact:** `apps/api/src/analysis-engine/providers/classical-chart-patterns/**` holds every disclosed tolerance/margin/score as a named, commented constant; `classical-chart-patterns.types.ts` defines `ChartPatternType`/`PatternPoint`/`ShapeCriterionCheck`/`ConfirmationStatus`/`PatternInvalidation`/`ChartPatternCandidate`, all confined to this directory. `apps/api/src/analysis-engine/analysis-engine.module.ts` registers `ClassicalChartPatternsProvider` as the fifth `ANALYSIS_PROVIDERS` entry, constructed with the same shared `SWING_DETECTOR`/`REGIME_CONTEXT` instances as all four prior Providers (deliberately not `INDICATOR_ENGINE`), with no dependency on any of them. `normalize()` added as a fifth fixture to the existing shared conformance suite (`normalize-vocabulary-conformance.spec.ts`, S1-012) — no change to that suite's own generic assertion logic. No change to the generic `AnalysisProvider`/`AnalysisProviderResult` contract, the Execution Engine, Lifecycle, Confluence Engine, or Normalized Vocabulary Schema.
-   **Related ADR:** ADR-006 (Provider contract mechanics), ADR-007 (Normalized Vocabulary, consumed via a new `normalize()` mapping) — this decision calibrates an already-approved design; it introduces no new technology, mechanism, or vocabulary dimension, consistent with the DEC-2026-009/011/012/013/014/015/016/017 precedent.
-   **Affected Components:** `apps/api/src/analysis-engine/providers/classical-chart-patterns`, `apps/api/src/analysis-engine/providers/normalize-vocabulary-conformance.spec.ts`, `apps/api/src/analysis-engine/analysis-engine.module.ts`.
-   **Implemented In:** S1-014.

## DEC-2026-019

-   **Date:** 2026-07-13
-   **Title:** Price Action Provider Calibration, State-Machine/Momentum Design Disclosure, and Golden-Dataset Sourcing
-   **Status:** Approved
-   **Decision Summary:** Records the implementation-time calibration anticipated by the S1-015 Sprint Brief's Missing Decisions section, plus two disclosed design clarifications surfaced during self-review. (1) `PriceActionProvider` is registered `tier: 'FAST'` — a single-pass classification over one key level, the same tiering basis as `IctSmcProvider` (S1-010), never a bounded multi-window search — and is the sixth `ANALYSIS_PROVIDERS` entry, constructed with the same shared `INDICATOR_ENGINE`/`SWING_DETECTOR`/`REGIME_CONTEXT` instances as every prior Provider (the first Provider to inject all three together), with no `dependsOn`. (2) The five reaction states are classified by a single deterministic sequential scan over the points strictly after the key level (`classifyReaction()`): the first point to reach the level decides `REJECTED_LEVEL` vs. a breakout candidate; every following point is then scanned in order for the first of a failure (a decisive close back across the level) or a held retest, whichever occurs first. **Clarification of the Sprint Brief's own Scope item 4 wording:** the wick-to-range ratio "clearing a disclosed threshold" is realized as this reading's own Detection Confidence/Quality Score measurement (`price-action-quality-scoring.util.ts`), not as an additional state-classification gate — the five states must remain exhaustive with no sixth fallback category for a rejection whose wick ratio happens to be small, so the ratio's role is to grade the rejection's own strength (feeding Detection Confidence, the weakness disclosure, and the boundary-proximity alternate mechanism), not to decide whether the state is reached at all. (3) Quality scoring: a rejection's own wick-to-range ratio; a breakout's own average of body-to-range and close-position ratios, plus ATR-relative clearance beyond the level (`INDICATOR_ENGINE.atr()`, S1-007, consumed directly by a Provider's own `analyze()` for the first time). (4) Momentum scoring (`MOMENTUM_FULL_SCORE_ATR_MULTIPLE = 2`) measures the ATR-relative net displacement from the breakout bar's own close to the most recent point's close, capped at a full score of 100. **Clarification of the Sprint Brief's own Scope item 6 wording ("net displacement from the key level... divided by current ATR and elapsed bars"):** the implemented measurement is anchored to the breakout bar itself, not the key level — the "current leg" this Provider reasons about is the move since the breakout actually began, not since the (potentially much older) swing that originally created the key level, which could include an arbitrarily long, momentum-irrelevant approach phase — and is expressed as a pure ATR-relative displacement rather than a per-bar rate, a simpler, equally defensible bounded V1 measurement, disclosed here rather than left as a silent deviation from the Sprint Brief's own illustrative wording. Continuation-vs-exhaustion compares the later half's own average body size against the earlier half's, among the points from the breakout through the most recent point, requiring at least `CONTINUATION_MIN_POINTS = 4` such points (else `null`, no fabricated classification) and a `CONTINUATION_MARGIN_RATIO = 1.2` divergence to read `CONTINUATION`/`EXHAUSTION` rather than `NEUTRAL_PACE`. (5) The bounded `interpretation[]` mechanism is a boundary-proximity check (`BOUNDARY_PROXIMITY_MARGIN_ATR = 0.15`), a genuinely different bounding mechanism from every prior Provider's own bounded multi-window search: a second, weaker alternate interpretation (`ALTERNATE_CONFIDENCE_FRACTION = 0.5` of the primary's own Interpretation/Regime-Adjusted Confidence) is disclosed only when the classification's own decisive point closed within that ATR margin of the key level. (6) Regime-Adjusted Confidence strengthens breakout/continuation readings when `volatilityState: 'HIGH'` and weakens them when `'LOW'` (`x1.15`/`x0.85`), while strengthening `REJECTED_LEVEL` readings when `'LOW'` and weakening them when `'HIGH'` — a genuinely state-dependent bifurcation of the same `volatilityState` axis, distinct from every prior Provider's own uniform or `trendState`-based rule; `APPROACHING_LEVEL` receives no adjustment at all, since it makes no directional claim yet to strengthen or weaken. `BREAKOUT_FAILED` is grouped under the breakout bifurcation (not the rejection one), since it is fundamentally a breakout attempt whose credibility still depends on genuine volatility expansion having occurred, even though its own direction is reported as the flipped, reversal-signal direction. (7) Methodology Confidence Ceiling for `'PRICE_ACTION'` is `70` — independently calibrated, reflecting a more mechanically deterministic classification than ICT/SMC's own interpretive bias (`60`) but a decentralized, multi-source sourcing profile rather than any single canonical, individually-authored text (distinct from Wyckoff's `85`, Elliott's `75`, Harmonic's `65`, Classical Chart Patterns' `80`). (8) `normalize()` populates a genuine, natively-computed `MOMENTUM` signal (parsed from a `[MOMENTUM_SCORE:...]` tag embedded in the primary interpretation's own summary) for the three directional (breakout) states only — the first Provider to populate this dimension from a purpose-built measurement rather than `NOT_APPLICABLE` or a proxy; `VOLUME` is always `NOT_APPLICABLE`, deliberately (not merely unimplemented), to preserve independence from Wyckoff's own volume-based methodology. **A disclosed technique, not a Missing Decision, adopted specifically to close off a recurring bug class named in this system's own prior sprints:** rather than string-matching bare state/direction words (which previously risked a substring-superset bug, e.g. an "UNCONFIRMED" reading's own text literally containing "CONFIRMED"), the primary summary embeds unambiguous, prefixed, bracketed tags (`[STATE:...]`, `[DIRECTION:...]`, `[MOMENTUM_SCORE:...]`) that `normalize()` matches on exactly, closing the bug class off by construction rather than requiring careful wording review each time. (9) Golden-dataset sourcing: unlike other registered methodologies, each of which already discloses its own reliance (in its own respective sprint) on a single, individually-authored canonical text, this methodology's own defining concepts are broadly corroborated across many decentralized, independently-authored sources rather than one single citable canonical reference; the golden-dataset tests instead reproduce the widely-taught qualitative instances every independent source agrees on — a decisive breakout subsequently retested and held, and a clean rejection with a dominant opposing wick. (10) `computationVersion`/`contractVersion` both `1.0.0`, the same convention as every prior Provider.
-   **Business Rationale:** The state-machine/quality/momentum calibration, boundary-proximity mechanism, Regime-Adjusted Confidence bifurcation, sourcing disclosure, and Methodology Confidence Ceiling are calibration choices within an already-approved Provider design (S1-015 Sprint Brief Scope items 1-14), not new architecture — analogous to the DEC-2026-009/011/012/013/014/015/016/017/018 precedent. The two wording clarifications (wick-ratio-as-confidence-not-gate; momentum anchored to the breakout point, expressed as pure displacement) are implementation-time refinements of the Sprint Brief's own illustrative language, made under the Engineering Authority already granted for this Sprint, not a change to any Scope item's own substance, and are disclosed here rather than left as a silent gap between the approved Brief and the delivered code. The bracketed-tag technique is a direct, disclosed application of a bug class this system's own prior sprints already identified and named, not a new mechanism requiring separate approval.
-   **Technical Impact:** `apps/api/src/analysis-engine/providers/price-action/**` holds every disclosed constant as a named, commented value; `price-action.types.ts` defines `KeyLevel`/`PriceActionState`/`ReactionClassification`/`QualityScore`/`ContinuationPace`/`PatternInvalidation`/`PriceActionReading`, all confined to this directory. `apps/api/src/analysis-engine/analysis-engine.module.ts` registers `PriceActionProvider` as the sixth `ANALYSIS_PROVIDERS` entry, constructed with the same shared `INDICATOR_ENGINE`/`SWING_DETECTOR`/`REGIME_CONTEXT` instances as every prior Provider, with no dependency on any of them. `normalize()` added as a sixth fixture to the existing shared conformance suite (`normalize-vocabulary-conformance.spec.ts`, S1-012) — no change to that suite's own generic assertion logic. No change to the generic `AnalysisProvider`/`AnalysisProviderResult` contract, the Execution Engine, Lifecycle, Confluence Engine, or Normalized Vocabulary Schema — this Provider is a pure consumer of the S1-007/S1-008/S1-012 frameworks. No new Prisma model, no new runtime dependency, no new HTTP surface.
-   **Related ADR:** ADR-005 (computation substrate calibration precedent — first Provider-direct `atr()` consumption), ADR-006 (Provider contract mechanics, Confidence taxonomy), ADR-007 (Normalized Vocabulary, consumed via a new `normalize()` mapping) — this decision calibrates an already-approved design; it introduces no new technology, mechanism, or vocabulary dimension, consistent with the DEC-2026-009/011/012/013/014/015/016/017/018 precedent.
-   **Affected Components:** `apps/api/src/analysis-engine/providers/price-action`, `apps/api/src/analysis-engine/providers/normalize-vocabulary-conformance.spec.ts`, `apps/api/src/analysis-engine/analysis-engine.module.ts`.
-   **Implemented In:** S1-015.

## DEC-2026-020

-   **Date:** 2026-07-13
-   **Title:** Supply & Demand Provider Zone-Detection Calibration, Health-Dimension Design, and Golden-Dataset Sourcing
-   **Status:** Approved
-   **Decision Summary:** Records the implementation-time calibration anticipated by the S1-016 Sprint Brief's Missing Decisions section. (1) `SupplyDemandProvider` is registered `tier: 'SLOW'` — a bounded linear scan across candle-count windows for base candidates, the same tiering basis as Elliott Wave/Harmonic Patterns/Classical Chart Patterns — and is the seventh `ANALYSIS_PROVIDERS` entry, constructed with only `INDICATOR_ENGINE`/`REGIME_CONTEXT` (the first Provider not to inject `SWING_DETECTOR` directly at all, since its own raw evidence is assessed entirely at the candle level; `RegimeContextService` already composes Swing Detection internally for its own trend read), with no `dependsOn`. (2) Base identification: `BASE_MAX_BODY_RATIO = 0.35` (a candle's own body-to-range ratio at or below this reads as indecisive), a bounded `MIN_BASE_CANDLES = 1`–`MAX_BASE_CANDLES = 5` window, and a shared tightness-bound pair (`BASE_TIGHT_ATR_MULTIPLE = 1`, full quality score; `BASE_LOOSE_ATR_MULTIPLE = 3`, the hard disqualification bound beyond which a base is discarded entirely, not merely scored lower). (3) Departure gating: `DEPARTURE_MIN_ATR_MULTIPLE = 1.5` (the hard qualification gate — a base with no qualifying departure is discarded, never returned as a low-confidence guess) and `DEPARTURE_FULL_SCORE_ATR_MULTIPLE = 3` (the soft full-score anchor). (4) The candle-index scan deliberately starts at `start = 1` (never `0`), so every candidate always has a preceding candle available for its own origin classification (`RALLY_BASE_RALLY`/`DROP_BASE_RALLY`/`RALLY_BASE_DROP`/`DROP_BASE_DROP`) — a disclosed candle-index edge condition, not a scope decision. (5) Freshness/mitigation health tracking: a touch episode groups consecutive touching subsequent points, any single non-touching point ending the current episode (an implicit `TOUCH_EPISODE_GAP = 1`); mitigation only ever worsens (`UNMITIGATED` → `PARTIALLY_MITIGATED` → `FULLY_MITIGATED`), never reverting once a subsequent close has cleared the distal line — a genuine implementation property, not merely a convention, is that `UNMITIGATED` only ever co-occurs with `FRESH` (any touch at all upgrades mitigation to at least `PARTIALLY_MITIGATED`), so only five of the nine combinatorially-possible freshness/mitigation pairs are ever reachable. (6) Disclosed decay multipliers applied to a zone's own quality score for Interpretation Confidence, keyed by its reachable freshness/mitigation combination: `FRESH`/`UNMITIGATED` = `1.0`; `TESTED_ONCE`/`PARTIALLY_MITIGATED` = `0.7`; `TESTED_MULTIPLE`/`PARTIALLY_MITIGATED` = `0.5`; `TESTED_ONCE`/`FULLY_MITIGATED` = `0.3`; `TESTED_MULTIPLE`/`FULLY_MITIGATED` = `0.15`. (7) The bounded `interpretation[]` mechanism selects at most one hypothesis per zone type (nearest `DEMAND` below current price, nearest `SUPPLY` above it, ordered nearer-first) — a genuinely different bounding rationale (sidedness, not ranking ambiguity) from every prior Provider's own bounded-hypothesis mechanism. (8) Regime-Adjusted Confidence strengthens a `DEMAND` zone when `RegimeContextResult.trendDirection` reads `'UP'` and weakens it when `'DOWN'`; strengthens a `SUPPLY` zone when it reads `'DOWN'` and weakens it when `'UP'` (`x1.15`/`x0.85`); `'UNKNOWN'` applies no adjustment at all — `trendDirection` is a genuinely distinct axis from every prior Provider's own `trendState`/`volatilityState`-keyed rule. (9) Methodology Confidence Ceiling for `'SUPPLY_DEMAND'` is `68` — independently calibrated, reflecting a sourcing profile similar to Price Action's own (decentralized retail-trading educators, no single institutional-grade canonical text) but with more inherent subjectivity in exact proximal/distal boundary placement across sources, placed just below Price Action's own `70`. (10) `normalize()` populates `LIQUIDITY` from the same direction as `TREND`/`STRUCTURE` (an unmitigated zone represents a concentration of resting institutional orders) — a deliberate, disclosed dimension-sharing with `IctSmcProvider`'s own `LIQUIDITY` use, since two Providers independently populating the same dimension from their own distinct methodologies is exactly how the Confluence Engine detects genuine cross-methodology agreement, not an independence violation; `MOMENTUM` and `VOLUME` are always `NOT_APPLICABLE`, deliberately, to preserve independence from Price Action's and Wyckoff's own respective native methodologies. The primary summary embeds `[TYPE:...]`/`[FRESHNESS:...]`/`[MITIGATION:...]` bracketed tags, the same disclosed technique adopted at S1-015 to close off the recurring substring-superset bug class by construction. (11) Golden-dataset sourcing: the same decentralized, multi-source-corroboration disclosure established at S1-015 for Price Action applies here (Sam Seiden and the "Online Trading Academy" curriculum, among many independent sources) — no single canonical primary text exists for this methodology either. (12) `computationVersion`/`vocabularySchemaVersion` both `1.0.0`, the same convention as every prior Provider.
-   **Business Rationale:** The base/departure/health/quality/confidence calibration, the one-per-side bounding rationale, and the Methodology Confidence Ceiling are calibration choices within an already-approved Provider design (S1-016 Sprint Brief Scope items 1-14), not new architecture — analogous to the DEC-2026-009/011/012/013/014/015/016/017/018/019 precedent. The deliberate non-injection of `SWING_DETECTOR` and the self-contained base-and-departure detection are direct implementations of this Sprint's own Architecture Requirements and Objective (avoiding conflation with `IctSmcProvider`'s own Order Blocks), not unilateral additions.
-   **Technical Impact:** `apps/api/src/analysis-engine/providers/supply-demand/**` holds every disclosed constant as a named, commented value; `supply-demand.types.ts` defines `ZoneType`/`ZoneOrigin`/`Freshness`/`MitigationStatus`/`ZoneBoundaries`/`RawZoneCandidate`/`ZoneQualityScore`/`ZoneInvalidation`/`SupplyDemandZone`, all confined to this directory. `apps/api/src/analysis-engine/analysis-engine.module.ts` registers `SupplyDemandProvider` as the seventh `ANALYSIS_PROVIDERS` entry, constructed with the shared `INDICATOR_ENGINE`/`REGIME_CONTEXT` instances (deliberately not `SWING_DETECTOR`), with no dependency on any of the six prior Providers. `normalize()` added as a seventh fixture to the existing shared conformance suite (`normalize-vocabulary-conformance.spec.ts`, S1-012) — no change to that suite's own generic assertion logic. No change to the generic `AnalysisProvider`/`AnalysisProviderResult` contract, the Execution Engine, Lifecycle, Confluence Engine, or Normalized Vocabulary Schema — this Provider is a pure consumer of the S1-007/S1-008/S1-012 frameworks. No new Prisma model, no new runtime dependency, no new HTTP surface.
-   **Related ADR:** ADR-005 (computation substrate calibration precedent — direct `atr()` consumption), ADR-006 (Provider contract mechanics, Confidence taxonomy), ADR-007 (Normalized Vocabulary, consumed via a new `normalize()` mapping, including a legitimate shared `LIQUIDITY` use) — this decision calibrates an already-approved design; it introduces no new technology, mechanism, or vocabulary dimension, consistent with the DEC-2026-009/011/012/013/014/015/016/017/018/019 precedent.
-   **Affected Components:** `apps/api/src/analysis-engine/providers/supply-demand`, `apps/api/src/analysis-engine/providers/normalize-vocabulary-conformance.spec.ts`, `apps/api/src/analysis-engine/analysis-engine.module.ts`.
-   **Implemented In:** S1-016.

## DEC-2026-021

-   **Date:** 2026-07-14
-   **Title:** Fibonacci Analysis Provider Confluence Calibration, Reaction-Classification Design, and Golden-Dataset Sourcing
-   **Status:** Approved
-   **Decision Summary:** Records the implementation-time calibration anticipated by the S1-017 Sprint Brief's Missing Decisions section. (1) `FibonacciAnalysisProvider` is registered `tier: 'SLOW'` — a bounded multi-leg scan and cross-leg clustering pass — and is the eighth `ANALYSIS_PROVIDERS` entry, constructed with the same shared `INDICATOR_ENGINE`/`SWING_DETECTOR`/`REGIME_CONTEXT` instances as every prior Provider (the second Provider to inject all three together, after `PriceActionProvider`, S1-015), with no `dependsOn`. (2) `MAX_SWINGS_FOR_LEGS = 4` bounds the scan to the last four swings, forming up to three consecutive legs; each leg's own `INDICATOR_ENGINE.fibonacciLevels()` call keeps only the true retracement ratios (`0.236`/`0.382`/`0.5`/`0.618`/`0.786`) and extension ratios (`1.272`/`1.618`), excluding the raw anchor ratios (`0`/`1`). (3) `CONFLUENCE_TOLERANCE_ATR_MULTIPLE = 0.5` governs cross-leg clustering — levels from *independent* legs within this ATR-relative distance of each other are read as genuine confluence; two ratios from the *same* leg are never clustered with each other regardless of proximity, since genuine confluence requires independent agreement, not a single leg's own internal ratio spacing. (4) `BREAK_MARGIN_ATR_MULTIPLE = 0.25` governs the touch/close-persistence reaction classification (`UNTESTED`/`RESPECTED`/`BROKEN`) — a genuinely different mechanism from `PriceActionProvider`'s own single-bar wick/body/close-position measurement (S1-015), deliberately not reused or duplicated. A candidate's own "role" (support vs. resistance) is determined from the first subsequent point's own close relative to the candidate's price, not the series' final close — an implementation-time correctness fix caught during self-review (see below). (5) Detection Confidence is the weaker of a confluence score (`CONFLUENCE_FULL_SCORE_LEG_COUNT = 3` independently-agreeing legs earns a full score of 100) and a precision score (a standalone level's own true-Fibonacci-ratio status scores `100`/`60` per the Indicator Engine's own disclosed `isTrueFibonacciRatio` distinction; a confluence zone's own score is inversely proportional to its contributing levels' own price spread, relative to the same `CONFLUENCE_TOLERANCE_ATR_MULTIPLE` bound used to form it). (6) Interpretation Confidence applies a disclosed reaction-state multiplier to the quality score: `UNTESTED = 1.0`, `RESPECTED = 1.15`, `BROKEN = 0.3`. (7) The bounded `interpretation[]` mechanism ranks every confluence zone and standalone level by proximity to current price, capped at `MAX_FIBONACCI_HYPOTHESES = 2` — a proximity-based bounding rationale, distinct from every prior Provider's own bounded-hypothesis mechanism (a single boundary-margin check, a one-per-side selection, or a score-ranked cap). (8) Regime-Adjusted Confidence strengthens a retracement-dominant reading when `volatilityState` reads `'LOW'` and an extension-dominant reading when it reads `'HIGH'` (`x1.15`/`x0.85`) — a genuinely distinct bifurcating variable (retracement-vs-extension level type) from every prior Provider's own rule, even where the underlying `volatilityState` axis is shared with `HarmonicPatternsProvider`'s and `PriceActionProvider`'s own rules. (9) Methodology Confidence Ceiling for `'FIBONACCI_ANALYSIS'` is `72` — independently calibrated, reflecting a sourcing profile genuinely different in shape from every prior Provider's own: the underlying ratio *mathematics* have an unusually solid, precisely-dated primary source (Leonardo of Pisa's "Liber Abaci," 1202, already cited by the Indicator Engine's own Fibonacci calculator), but the confluence-zone *trading application* itself is decentralized across independent retail-trading educators, the same sourcing category as several other registered methodologies. (10) `normalize()` embeds `[DIRECTION:...]`/`[REACTION:...]` bracketed tags (the same technique adopted at S1-015 to close off the recurring substring-superset bug class by construction); `TREND`/`STRUCTURE` both read the primary reading's own direction (already flipped when `BROKEN`); `CONFIRMATION` matches only for `RESPECTED`; `MOMENTUM` and `VOLUME` are always `NOT_APPLICABLE`, deliberately, to preserve independence from `PriceActionProvider`'s and `WyckoffProvider`'s own respective native methodologies. (11) Golden-dataset sourcing: the same decentralized, multi-source-corroboration disclosure established at S1-015/S1-016 applies to the confluence-zone trading application (no single canonical text), while the ratio mathematics themselves cite Leonardo of Pisa directly. (12) `computationVersion`/`vocabularySchemaVersion` both `1.0.0`, the same convention as every prior Provider. **One disclosed self-review fix, not a Missing Decision:** the first draft determined a candidate's own support/resistance role from the series' final close (the same value used for proximity ranking), which made a `BROKEN` classification structurally unreachable whenever only one subsequent point existed (role would always trivially match wherever that single point's own close landed). Fixed by determining role from the *first* subsequent point's own close instead, while proximity ranking continues to use the true final close — the two are already distinct once more than one subsequent point exists, exactly the golden-dataset broken-level scenario requires.
-   **Business Rationale:** The confluence/reaction/quality/confidence calibration, the proximity-based bounding rationale, and the Methodology Confidence Ceiling are calibration choices within an already-approved Provider design (S1-017 Sprint Brief Scope items 1-13), not new architecture — analogous to the DEC-2026-009/011/012/013/014/015/016/017/018/019/020 precedent. The role-determination fix is a correctness repair to an already-approved mechanism (Scope item 5), caught and fixed during this Sprint's own self-review before any test was written against the buggy behavior, not a design change requiring escalation.
-   **Technical Impact:** `apps/api/src/analysis-engine/providers/fibonacci-analysis/**` holds every disclosed constant as a named, commented value; `fibonacci-analysis.types.ts` defines `LevelType`/`RawFibonacciLevel`/`ReactionState`/`FibonacciCandidate`/`FibonacciQualityScore`/`FibonacciInvalidation`/`FibonacciDirection`/`FibonacciHypothesis`, all confined to this directory. `apps/api/src/analysis-engine/analysis-engine.module.ts` registers `FibonacciAnalysisProvider` as the eighth `ANALYSIS_PROVIDERS` entry, constructed with the same shared `INDICATOR_ENGINE`/`SWING_DETECTOR`/`REGIME_CONTEXT` instances as every prior Provider, with no dependency on any of them. `normalize()` added as an eighth fixture to the existing shared conformance suite (`normalize-vocabulary-conformance.spec.ts`, S1-012) — no change to that suite's own generic assertion logic. No change to the generic `AnalysisProvider`/`AnalysisProviderResult` contract, the Execution Engine, Lifecycle, Confluence Engine, or Normalized Vocabulary Schema — this Provider is a pure consumer of the S1-007/S1-008/S1-012 frameworks, and the second direct consumer of `INDICATOR_ENGINE.fibonacciLevels()` (after `ElliottWaveProvider`, S1-011) for a genuinely different analytical purpose. No new Prisma model, no new runtime dependency, no new HTTP surface.
-   **Related ADR:** ADR-005 (computation substrate calibration precedent — second direct `fibonacciLevels()` consumer), ADR-006 (Provider contract mechanics, Confidence taxonomy), ADR-007 (Normalized Vocabulary, consumed via a new `normalize()` mapping) — this decision calibrates an already-approved design; it introduces no new technology, mechanism, or vocabulary dimension, consistent with the DEC-2026-009/011/012/013/014/015/016/017/018/019/020 precedent.
-   **Affected Components:** `apps/api/src/analysis-engine/providers/fibonacci-analysis`, `apps/api/src/analysis-engine/providers/normalize-vocabulary-conformance.spec.ts`, `apps/api/src/analysis-engine/analysis-engine.module.ts`.
-   **Implemented In:** S1-017.

## DEC-2026-022

-   **Date:** 2026-07-14
-   **Title:** S1-018 (VSA) Architecture Prerequisite Check — No Hard Dependency on `WyckoffProvider` Required
-   **Status:** Approved
-   **Decision Summary:** Before opening S1-018, the Architecture Team required an objective check of whether VSA (Volume Spread Analysis) can be implemented against the architecture exactly as it stands, rather than assuming the anticipatory framing recorded at S1-009 ("VSA depends on Wyckoff's active range output," `22_ANALYSIS_ENGINE_ARCHITECTURE.md` "Analysis Provider Framework — Dependency declaration"; ADR-006's own Context/Consequences/Alternatives sections) still holds. Two things were verified directly against the current source, not assumed from that historical note: (1) `ProviderExecutionService.scheduleNewRun()` (`provider-execution.service.ts`) resolves `dependsOn` only as a topological-sort/participation gate — it awaits each declared dependency's outcome only to check whether it *participated*, and never passes that dependency's `AnalysisProviderResult` into the dependent Provider's own `analyze(series: MarketSeries)` call, whose signature accepts only `series`. No data channel between Providers exists today; `dependsOn` has never been exercised by any of the eight registered Providers. (2) VSA's own actual analytical mechanism (Tom Williams/Anna Coulling: per-bar volume-vs-spread-vs-close-position reasoning — "No Demand," "No Supply," "Stopping Volume," "Upthrust," "Effort vs. Result" divergence) is bar-level and regime-level, not schematic-level — it does not, in the source methodology itself, require a pre-identified Wyckoff accumulation/distribution range as an input; it is applied to any bar in any market condition. Every raw input VSA needs is already present in the existing shared substrate at parity with every other registered Provider: `MarketSeriesPoint.volume`/`open`/`high`/`low`/`close` (spread and close-position-within-bar), `INDICATOR_ENGINE.atr()` (spread normalization, the same reuse pattern as `PriceActionProvider`/`SupplyDemandProvider`), `SWING_DETECTOR` (key-level context), and `REGIME_CONTEXT` (`trendState`/`volatilityState` gating). **Conclusion: no genuine architectural blocker exists.** S1-018 will register `VsaProvider` as a fully self-contained Provider with no `dependsOn`, mirroring the independence pattern every Provider since S1-010 has actually followed in practice (verified each time by its own Independence Boundary Test) — reversing only the *anticipatory* framing recorded at S1-009/ADR-006, not any mechanism actually built since. The still-open question ADR-006 originally raised — how a dependent Provider would receive an upstream Provider's structured data beyond the generic contract — remains genuinely unsolved and is correctly left deferred (no Provider yet needs it, since none declares `dependsOn`); it is not a blocker for S1-018 specifically, because VSA does not need it.
-   **Business Rationale:** Deciding S1-018's dependency shape before drafting its Sprint Brief avoids repeating the S1-009-era assumption uncritically into a new sprint's own Scope, and avoids proposing a framework change (a Provider-to-Provider data channel) that nothing registered actually requires. This keeps S1-018 consistent with the Architecture Team's own Roadmap Order instruction that VSA proceed "once its architectural prerequisites are satisfied" — this check is that satisfaction, recorded before implementation begins, not inferred after the fact.
-   **Technical Impact:** No code change. This decision governs S1-018's own upcoming Sprint Brief: `VsaProvider` will be specified with no `dependsOn` entry, constructed from `INDICATOR_ENGINE`/`SWING_DETECTOR`/`REGIME_CONTEXT` (the exact subset each Provider's own methodology needs, per the established per-Provider injection precedent), with its own Independence Boundary Test scanning for literal references to every one of the seven prior Providers, including `wyckoff`/`WYCKOFF` specifically (the one prior Provider VSA was historically assumed to require).
-   **Related ADR:** ADR-006 (Provider contract mechanics; this decision determines that the `dependsOn`/topological-sort mechanism ADR-006 specifies is not required for VSA and remains unexercised by any registered Provider) — introduces no new technology, mechanism, or contract change; a readiness determination, not an architecture revision.
-   **Affected Components:** None yet (informs the S1-018 Sprint Brief only).
-   **Implemented In:** S1-018 (pending).

## DEC-2026-023

-   **Date:** 2026-07-14
-   **Title:** VSA Provider Bar-Classification Calibration, Signal-Detection Design, and Golden-Dataset Sourcing
-   **Status:** Approved
-   **Decision Summary:** Records the implementation-time calibration anticipated by the S1-018 Sprint Brief's Missing Decisions section. (1) `VsaProvider` is registered `tier: 'SLOW'` — a bounded scan across a recent-bar window with per-bar classification, the same tiering category as every other bounded-multi-window-search Provider — and is the ninth `ANALYSIS_PROVIDERS` entry, constructed with the same shared `INDICATOR_ENGINE`/`SWING_DETECTOR`/`REGIME_CONTEXT` instances already injected together by several prior Providers, with no `dependsOn` (per DEC-2026-022's own prerequisite finding). (2) A single shared `LOOKBACK_WINDOW = 10` serves both as the trailing-volume-baseline period and the local-extreme lookback for Upthrust/Shakeout — a simpler design than two independently-sized windows, disclosed rather than silently assumed. `MAX_BARS_FOR_VSA_SCAN = 15` bounds the recent-bar window scanned for qualifying signals; the minimum series length for any classification at all is `LOOKBACK_WINDOW + 1 = 11` bars. (3) Spread classification: `NARROW_SPREAD_ATR_MULTIPLE = 0.5`, `WIDE_SPREAD_ATR_MULTIPLE = 1.5` (a bar's own high-low range relative to ATR). Volume classification: `LOW_VOLUME_RATIO = 0.7`, `HIGH_VOLUME_RATIO = 1.5`, `ULTRA_HIGH_VOLUME_RATIO = 2.5` (relative to a trailing average computed strictly from preceding bars, never the bar itself). Close-position classification: `NEAR_HIGH_CLOSE_POSITION = 0.7`, `NEAR_LOW_CLOSE_POSITION = 0.3`. (4) The five named signal types are checked in a disclosed priority order so no bar is ever double-classified: Upthrust and Shakeout (each requiring a genuine new local extreme against `LOOKBACK_WINDOW`'s own preceding highs/lows) are checked first; Stopping Volume is checked only when neither matched (no new local extreme was made); No Demand/No Supply are checked last, gated additionally by the Regime/Context Service's own `trendDirection` (an active up-move or down-move, respectively) and structurally mutually exclusive with the three `WIDE`-spread signals by requiring `NARROW` spread. Upthrust and Shakeout are themselves mutually exclusive by construction, since their own required close positions (`NEAR_LOW` vs. `NEAR_HIGH`) cannot both hold on the same bar. (5) The bounded `interpretation[]` mechanism ranks every detected signal by recency (bar index, most recent first), capped at `MAX_VSA_HYPOTHESES = 2` — a recency-based bounding rationale, distinct from every prior Provider's own (a boundary-margin check, a one-per-side selection, a proximity-to-price ranking, or a score-ranked cap). (6) Detection Confidence is a single, uniform anomaly-magnitude formula applied regardless of signal category: `min(100, (|volumeRatio - 1| + |spreadAtrRatio - 1|) * 40)` — the sharper a bar's own combined deviation from "perfectly average" (ratio of `1`), the stronger the raw detection, whether that deviation is a climax-type bar's own excess or a quiet-type bar's own depression. (7) Interpretation Confidence strengthens Detection Confidence by `SWING_PROXIMITY_MULTIPLIER = 1.2` when the signal's own bar occurred within `SWING_PROXIMITY_TOLERANCE_ATR_MULTIPLE = 0.5` (ATR-relative) of a Swing-Detector-identified swing high/low, unchanged otherwise. (8) Regime-Adjusted Confidence strengthens a climax-type signal (Upthrust/Shakeout/Stopping Volume) by `1.15`/weakens by `0.85` when `volatilityState` reads `HIGH`/`LOW` respectively; strengthens a quiet-type signal (No Demand/No Supply) by the same multipliers under the reversed condition (`LOW`/`HIGH`) — a genuinely distinct bifurcating variable (signal category) from every prior Provider's own rule, even where the underlying `volatilityState` axis is reused. (9) Methodology Confidence Ceiling for `'VSA'` is `74` — independently calibrated, reflecting a sourcing profile distinct in shape from every prior Provider's own: a single identifiable founder's own primary text (Tom Williams' "Master the Markets") corroborated by a second identifiable author's own widely-cited text (Anna Coulling's "A Complete Guide to Volume Price Analysis") — stronger sourcing than a fully decentralized multi-educator methodology, but newer and less classically-settled than the oldest registered methodology's own canonical text. (10) `normalize()` populates `VOLUME` natively for the first time by any registered Provider (every prior Provider deliberately left it `NOT_APPLICABLE` to preserve independence from this Provider's own future territory — the disclosed, anticipated exception); `TREND`/`STRUCTURE` from the primary signal's own implied direction; `CONFIRMATION` matches direction only for a climax-type signal; `MOMENTUM`/`LIQUIDITY`/`VOLATILITY` always `NOT_APPLICABLE`. The primary summary embeds `[SIGNAL:...]`/`[CATEGORY:...]`/`[DIRECTION:...]` bracketed tags, the same disclosed technique adopted at S1-015 to close off the recurring substring-superset bug class by construction. (11) Golden-dataset sourcing: the same dual-sourcing disclosure named in (9) applies — a worked No Demand instance and a worked Upthrust instance, matched against Tom Williams' and Anna Coulling's own widely-taught qualitative descriptions, not a specific numbered figure from either text. (12) `computationVersion`/`vocabularySchemaVersion` both `1.0.0`, the same convention as every prior Provider. **Two disclosed self-review fixes, not Missing Decisions:** (a) before the Independence Boundary Test was even run, doc comments in `vsa.types.ts`, `vsa-confidence.util.ts`, and `vsa.provider.ts` were found, on self-review, to name the earlier, structurally-related methodology directly while explaining this Sprint's own disclosed shared-terminology exception — reworded proactively to describe the property without naming that Provider. (b) The Independence Boundary Test's own first real run still failed against `vsa-confidence.util.ts`: a separate doc comment on the Regime-Adjusted Confidence rule, explaining that its own `volatilityState` axis is reused from other Providers' rules, named three of them directly (the same axis-reuse disclosure pattern `FibonacciAnalysisProvider`'s own DEC-2026-021 had already established wording for, missed on the first pass here). Fixed by rewording to "other already-registered Providers' own rules" — the same recurring false-positive class caught at S1-007, S1-009, S1-010, S1-011, and S1-016: a doc comment disclosing a legitimate relationship to other Providers can itself trip a literal-term boundary test.
-   **Business Rationale:** The bar-classification/signal-detection/confidence calibration, the recency-based bounding rationale, and the Methodology Confidence Ceiling are calibration choices within an already-approved Provider design (S1-018 Sprint Brief Scope items 1-11), not new architecture — analogous to the DEC-2026-009/011/012/013/014/015/016/017/018/019/020/021 precedent. The unified `LOOKBACK_WINDOW` (serving both the trailing-volume baseline and the local-extreme check) and the uniform anomaly-magnitude formula (serving both climax-type and quiet-type signals identically) are implementation-time simplifications within this Sprint's own Scope, not scope expansions.
-   **Technical Impact:** `apps/api/src/analysis-engine/providers/vsa/**` holds every disclosed constant as a named, commented value; `vsa.types.ts` defines `SpreadClassification`/`VolumeClassification`/`ClosePosition`/`BarDirection`/`ClassifiedBar`/`VsaSignalType`/`VsaSignal`/`VsaInvalidation`/`VsaHypothesis`, all confined to this directory. `apps/api/src/analysis-engine/analysis-engine.module.ts` registers `VsaProvider` as the ninth `ANALYSIS_PROVIDERS` entry, constructed with the shared `INDICATOR_ENGINE`/`SWING_DETECTOR`/`REGIME_CONTEXT` instances, with no dependency on any of the eight prior Providers. `normalize()` added as a ninth fixture to the existing shared conformance suite (`normalize-vocabulary-conformance.spec.ts`, S1-012) — no change to that suite's own generic assertion logic. No change to the generic `AnalysisProvider`/`AnalysisProviderResult` contract, the Execution Engine (including its `dependsOn` topological-sort mechanism, left genuinely unexercised), Lifecycle, Confluence Engine, or Normalized Vocabulary Schema — this Provider is a pure consumer of the S1-007/S1-008/S1-012 frameworks. No new Prisma model, no new runtime dependency, no new HTTP surface.
-   **Related ADR:** ADR-005 (computation substrate calibration precedent — direct `atr()` consumption), ADR-006 (Provider contract mechanics, Confidence taxonomy), ADR-007 (Normalized Vocabulary, consumed via a new `normalize()` mapping, including the first native `VOLUME` population) — this decision calibrates an already-approved design; it introduces no new technology, mechanism, or vocabulary dimension, consistent with the DEC-2026-009/011/012/013/014/015/016/017/018/019/020/021 precedent.
-   **Affected Components:** `apps/api/src/analysis-engine/providers/vsa`, `apps/api/src/analysis-engine/providers/normalize-vocabulary-conformance.spec.ts`, `apps/api/src/analysis-engine/analysis-engine.module.ts`.
-   **Implemented In:** S1-018.

# Rules

-   Every architectural decision must have a Decision Log entry.
-   Superseded decisions remain in history.
-   Entries are append-only; never rewrite history.
-   Only the Architecture Team may approve or close a decision.

# Related Documents

-   05_ARCHITECTURE.md
-   12_ADR_INDEX.md
-   09_PROJECT_BRAIN.md
