# 09_PROJECT_BRAIN

**Document ID:** ZOS-009\
**Version:** 1.0.0\
**Status:** Living Document\
**Owner:** Architecture Team

------------------------------------------------------------------------

# Purpose

Project Brain is the live operational memory of Zenith. Unlike the other
ZOS documents, this file is updated after approved sprint closures and
always reflects the current project state.

# Current Status

## Project

-   Name: Zenith
-   Version: 0.1.0
-   Current Milestone: M2 — Product Expansion — In Progress. M1 — Core Platform (S1-002 through S1-018, including the nine-Provider Analysis Provider Architecture Phase) is complete and formally closed. M2 began with Phase 2's product-definition work and its first two implementation Sprints, S1-019 (Dashboard Backend Foundation) and S1-020 (Morning Brief Backend / Narrative Composer), both complete.
-   Current Phase: Phase 1 — Engineering Foundation (per this document's own execution-tracking convention; S1-019/S1-020 are normal engineering Sprints, distinct from Phase 2's own non-Sprint documentation work, which remains closed). Phase 2 (`24_ZENITH_PRODUCT_CONSTITUTION.md`, `25_PRODUCT_BLUEPRINT.md`, `26_DASHBOARD_HOME_SPECIFICATION.md`, `27_ZENITH_EXPERIENCE_LANGUAGE.md`, all frozen) authorized no implementation of its own but supplied the implementation authority both Sprints were built against.
-   Current Sprint: None active. S1-022 (Dashboard Production UI V1) closed 2026-07-14 — the first production frontend implementation, not itself a Blueprint Implementation Order step (those remain backend-focused). Per `25_PRODUCT_BLUEPRINT.md` §10's own Implementation Order, the next candidate backend work is Watchlist/Portfolio annotation enrichment (steps 4-5, reusing `InstrumentReadingService`, already exported for this purpose) and Morning Brief's own full screen assembly (step 6) — none has been proposed or approved; this remains a fresh Architecture Team decision.

## Completed Sprints

-   S1-001 — Foundation Setup. Closed 2026-07-12. Sprint Brief: `documentation/zos/sprints/S1-001_SPRINT_BRIEF.md`. Completion Report: `documentation/ai/S1-001_COMPLETION_REPORT.md`. Final implementation commit: `f29785a`.
-   S1-002 — User Management Foundation. Closed 2026-07-12. Sprint Brief: `documentation/zos/sprints/S1-002_SPRINT_BRIEF.md`. Completion Report: `documentation/ai/S1-002_COMPLETION_REPORT.md`. Final implementation commits: `6abeafc`, `5325c44`.
-   S1-003 — Trading Catalog & User Watchlist Foundation. Closed 2026-07-12. Sprint Brief: `documentation/zos/sprints/S1-003_SPRINT_BRIEF.md`. Completion Report: `documentation/ai/S1-003_COMPLETION_REPORT.md`. Final implementation commits: `bbc7cd3`, `58e75f4`.
-   S1-004 — Position & Portfolio Management Foundation. Closed 2026-07-12. Sprint Brief: `documentation/zos/sprints/S1-004_SPRINT_BRIEF.md`. Completion Report: `documentation/ai/S1-004_COMPLETION_REPORT.md`. Final implementation commits: `94c8987`, `26611d9`.
-   S1-005 — Market Data Foundation. Closed 2026-07-12. Sprint Brief: `documentation/zos/sprints/S1-005_SPRINT_BRIEF.md`. Completion Report: `documentation/ai/S1-005_COMPLETION_REPORT.md`. Final implementation commits: `db4a352`, `be2680f`.
-   S1-006 — Trading Analytics Foundation. Closed 2026-07-12. Sprint Brief: `documentation/zos/sprints/S1-006_SPRINT_BRIEF.md`. Completion Report: `documentation/ai/S1-006_COMPLETION_REPORT.md`. See the Sprint Brief's Sprint Closure section for final implementation commits.
-   S1-007 — Analysis Engine Foundation: Indicator Engine, Swing Detection Infrastructure & Regime/Context Service. Closed 2026-07-12. Sprint Brief: `documentation/zos/sprints/S1-007_SPRINT_BRIEF.md`. Completion Report: `documentation/ai/S1-007_COMPLETION_REPORT.md`. See the Sprint Brief's Sprint Closure section for final implementation commits.
-   S1-008 — Analysis Provider Framework. Closed 2026-07-13. Sprint Brief: `documentation/zos/sprints/S1-008_SPRINT_BRIEF.md`. Task Breakdown: `documentation/ai/S1-008_TASK_BREAKDOWN.md`. Completion Report: `documentation/ai/S1-008_COMPLETION_REPORT.md`. See the Sprint Brief's Sprint Closure section for final implementation commits.
-   S1-009 — Wyckoff Method Analysis Provider. Closed 2026-07-13. Sprint Brief: `documentation/zos/sprints/S1-009_SPRINT_BRIEF.md`. Task Breakdown: `documentation/ai/S1-009_TASK_BREAKDOWN.md`. Completion Report: `documentation/ai/S1-009_COMPLETION_REPORT.md`. See the Sprint Brief's Sprint Closure section for final implementation commits.
-   S1-010 — ICT / Smart Money Concepts (SMC) Analysis Provider. Closed 2026-07-13. Sprint Brief: `documentation/zos/sprints/S1-010_SPRINT_BRIEF.md`. Task Breakdown: `documentation/ai/S1-010_TASK_BREAKDOWN.md`. Completion Report: `documentation/ai/S1-010_COMPLETION_REPORT.md`. See the Sprint Brief's Sprint Closure section for final implementation commits.
-   S1-011 — Elliott Wave Analysis Provider. Closed 2026-07-13. Sprint Brief: `documentation/zos/sprints/S1-011_SPRINT_BRIEF.md`. Task Breakdown: `documentation/ai/S1-011_TASK_BREAKDOWN.md`. Completion Report: `documentation/ai/S1-011_COMPLETION_REPORT.md`. See the Sprint Brief's Sprint Closure section for final implementation commits.
-   S1-012 — Confluence Engine. Closed 2026-07-13. Sprint Brief: `documentation/zos/sprints/S1-012_SPRINT_BRIEF.md`. Task Breakdown: `documentation/ai/S1-012_TASK_BREAKDOWN.md`. Completion Report: `documentation/ai/S1-012_COMPLETION_REPORT.md`. See the Sprint Brief's Sprint Closure section for final implementation commits.
-   S1-013 — Harmonic Patterns Analysis Provider. Closed 2026-07-13. Sprint Brief: `documentation/zos/sprints/S1-013_SPRINT_BRIEF.md`. Task Breakdown: `documentation/ai/S1-013_TASK_BREAKDOWN.md`. Completion Report: `documentation/ai/S1-013_COMPLETION_REPORT.md`. See the Sprint Brief's Sprint Closure section for final implementation commits.
-   S1-014 — Classical Chart Patterns Analysis Provider. Closed 2026-07-13. Sprint Brief: `documentation/zos/sprints/S1-014_SPRINT_BRIEF.md`. Task Breakdown: `documentation/ai/S1-014_TASK_BREAKDOWN.md`. Completion Report: `documentation/ai/S1-014_COMPLETION_REPORT.md`. See the Sprint Brief's Sprint Closure section for final implementation commits.
-   S1-015 — Price Action Analysis Provider. Closed 2026-07-13. Sprint Brief: `documentation/zos/sprints/S1-015_SPRINT_BRIEF.md`. Task Breakdown: `documentation/ai/S1-015_TASK_BREAKDOWN.md`. Completion Report: `documentation/ai/S1-015_COMPLETION_REPORT.md`. See the Sprint Brief's Sprint Closure section for final implementation commits.
-   S1-016 — Supply & Demand Analysis Provider. Closed 2026-07-13. Sprint Brief: `documentation/zos/sprints/S1-016_SPRINT_BRIEF.md`. Task Breakdown: `documentation/ai/S1-016_TASK_BREAKDOWN.md`. Completion Report: `documentation/ai/S1-016_COMPLETION_REPORT.md`. See the Sprint Brief's Sprint Closure section for final implementation commits.
-   S1-017 — Fibonacci Analysis Provider. Closed 2026-07-14. Sprint Brief: `documentation/zos/sprints/S1-017_SPRINT_BRIEF.md`. Task Breakdown: `documentation/ai/S1-017_TASK_BREAKDOWN.md`. Completion Report: `documentation/ai/S1-017_COMPLETION_REPORT.md`. See the Sprint Brief's Sprint Closure section for final implementation commits.
-   S1-018 — VSA (Volume Spread Analysis) Provider. Closed 2026-07-14. Sprint Brief: `documentation/zos/sprints/S1-018_SPRINT_BRIEF.md`. Task Breakdown: `documentation/ai/S1-018_TASK_BREAKDOWN.md`. Completion Report: `documentation/ai/S1-018_COMPLETION_REPORT.md`. See the Sprint Brief's Sprint Closure section for final implementation commits.
-   S1-019 — Dashboard Backend Foundation. Closed 2026-07-14. Sprint Brief: `documentation/zos/sprints/S1-019_SPRINT_BRIEF.md`. Task Breakdown: `documentation/ai/S1-019_TASK_BREAKDOWN.md` (AI-045). Completion Report: `documentation/ai/S1-019_COMPLETION_REPORT.md` (AI-046).
-   S1-020 — Morning Brief Backend (Narrative Composer). Closed 2026-07-14. Sprint Brief: `documentation/zos/sprints/S1-020_SPRINT_BRIEF.md`. Task Breakdown: `documentation/ai/S1-020_TASK_BREAKDOWN.md` (AI-047). Completion Report: `documentation/ai/S1-020_COMPLETION_REPORT.md` (AI-048).
-   S1-021 — Developer Preview V1 (Execution Validation). Closed 2026-07-14. No Sprint Brief/Task Breakdown/Completion Report produced, per this Sprint's own explicit instruction ("do not write new documentation unless implementation genuinely exposes a missing architectural decision" — none was). See Active Modules (`apps/web`) above for scope; full UX/performance/architecture observations delivered directly to Product Leadership in-session, not filed as a ZOS document.
-   S1-022 — Dashboard Production UI V1. Closed 2026-07-14. No Sprint Brief/Task Breakdown/Completion Report produced — no backend or architectural change was made (frontend UI/CSS only). See Active Modules (`apps/web`) above for scope; full UI-decision rationale and non-binding engineering observations delivered directly to Product Leadership in-session, not filed as a ZOS document.

## Active Modules

-   API (`apps/api`) — NestJS foundation: health check, Swagger, Pino logging, JWT authentication with real user registration/login (Argon2id); trading catalog (exchanges, markets, assets) with `ADMIN`-gated mutation and open read; user watchlists, watchlist items, and favourite assets with strict per-user ownership scoping; user-owned portfolios and positions with transaction-derived quantity/average-cost/realized-P&L accounting, concurrency-safe via row-locked database transactions; market data (symbol search, asset lookup, quotes, daily candles) sourced through a provider-agnostic interface, currently backed only by a simulated provider (ADR-003 — not real market data), cached in PostgreSQL, rate-limited, retried with backoff, and kept fresh for tracked assets via a `@nestjs/schedule` background job; a read-only Trading Analytics layer (portfolio/position P&L, risk exposure, a rule-based Portfolio Health Score, Decision Readiness, Data Quality/Confidence) composing the Portfolio/Position/Market Data services, computed live on every request with zero persisted history; an internal, non-HTTP Analysis Engine foundation (`analysis-engine` module) — a `MarketSeries` Anti-Corruption Layer, a nine-calculator Indicator Engine (SMA, EMA, RSI, MACD, Bollinger Bands, ATR, ADX, Fibonacci, Donchian Channel), a Swing Detection Infrastructure (BOS/CHoCH), a Regime/Context Service, an Analysis Provider Framework (S1-008 — Provider registry and Execution Engine: dependency resolution, fast/slow tiering, partial-failure reporting, a circuit breaker, Provider Lifecycle gating), (S1-009) the first real Analysis Provider — `WyckoffProvider`, reading price structure through the Wyckoff Method's Accumulation/Distribution Schematic #1 (trading-range identification, the full 16-event vocabulary, bounded multi-hypothesis Phase A-E classification, the four-part Confidence taxonomy) — (S1-010) the second real Analysis Provider — `IctSmcProvider`, reading Order Blocks, Fair Value Gaps, and Liquidity Sweeps over the same shared substrate, fully independent of `WyckoffProvider` (no `dependsOn`, mechanically-verified zero shared code), with its own lower Methodology Confidence Ceiling and a mirror-image Regime-Adjusted Confidence rule — (S1-011) the third real Analysis Provider — `ElliottWaveProvider`, generating a bounded 5-wave motive (impulse) count via Elliott's Three Rules as hard invalidation and Fibonacci-guideline proximity as soft ranking (the first real consumer of the Indicator Engine's `fibonacciLevels()` calculator, idle since S1-007), fully independent of both prior Providers, with its own independently-calibrated Methodology Confidence Ceiling and Regime-Adjusted Confidence rule — and (S1-012) the first real Confluence Engine (`ConfluenceService`, `CONFLUENCE_ENGINE` token): a Normalized Vocabulary Schema (seven dimensions — Trend, Momentum, Liquidity, Structure, Volatility, Volume, Confirmation), completing each Provider's own previously-placeholder `normalize()` method (ADR-006/007) with a decentralized, per-Provider mapping confined to each Provider's own module directory; methodology-family-aware, dimension-level aggregation (`O(Providers × 7)`, never pairwise) reporting genuine cross-methodology agreement and disagreement, with bounded top-3 contributor attribution per side, never voting or averaging confidence into a false consensus — and (S1-013) the fourth real Analysis Provider — `HarmonicPatternsProvider`, reading precise Fibonacci-ratio geometry across five pivot points (X, A, B, C, D) against four named pattern ratio tables (Gartley, Bat, Butterfly, Crab), each checked independently so a single swing window may honestly produce zero, one, or multiple never-merged pattern-type hypotheses, with its own `volatilityState`-based Regime-Adjusted Confidence rule (a genuinely distinct axis from every prior Provider's own `trendState`-based rule) — and (S1-014) the fifth real Analysis Provider — `ClassicalChartPatternsProvider`, reading Head and Shoulders (+ Inverse) and Double Top/Bottom reversal shapes via two independent bounded swing-window scans, with a genuinely novel confirmation-status Interpretation Confidence (unconfirmed/confirmed/volume-confirmed, per Edwards & Magee's own emphasis on volume-expanding breakouts) distinct in kind from every prior Provider's own hard/soft split — and (S1-015) the sixth real Analysis Provider — `PriceActionProvider`, reading market behavior (rejection strength, breakout quality, retest quality, momentum, continuation-vs-exhaustion) at a single, most-recent key level via one deterministic sequential scan into five hard states, never a named candlestick-pattern lookup, the first Provider to inject `INDICATOR_ENGINE`/`SWING_DETECTOR`/`REGIME_CONTEXT` together and the first to populate `normalize()`'s `MOMENTUM` dimension from a genuine, purpose-built measurement rather than `NOT_APPLICABLE` or a proxy — and (S1-016) the seventh real Analysis Provider — `SupplyDemandProvider`, reading institutional Supply and Demand zones (a consolidating base immediately followed by an impulsive departure, proximal/distal boundaries, independently-tracked freshness/mitigation health decay) via a bounded linear scan, self-contained and deliberately never reusing `IctSmcProvider`'s own Order Block utilities despite a shared retail-trading lineage, and the first Provider not to inject `SWING_DETECTOR` directly at all — and (S1-017) the eighth real Analysis Provider — `FibonacciAnalysisProvider`, reading Fibonacci retracements and extensions across a bounded set of recent swing legs, clustering same-price-region levels drawn from *different* legs into cross-leg confluence candidates, then classifying each candidate's own subsequent support/resistance reaction (untested/respected/broken) rather than merely calculating ratios, fully independent of both `ElliottWaveProvider`'s wave-count-guideline use and `HarmonicPatternsProvider`'s named-XABCD-pattern use of Fibonacci ratios despite sharing the same underlying `fibonacciLevels()` calculator and conceptual ratio vocabulary — and (S1-018) the ninth real Analysis Provider — `VsaProvider`, reading each bar's own effort-vs-result relationship (volume relative to a trailing average, spread relative to ATR, close position within the bar) into five named signals (No Demand, No Supply, Upthrust, Shakeout, Stopping Volume) via a disclosed priority-ordered detection mechanism, honestly reusing several of `WyckoffProvider`'s own historically accurate terms while sharing zero code or file-level coupling with it (per DEC-2026-022's own architecture prerequisite check, requiring no `dependsOn`), and the first Provider to populate `normalize()`'s `VOLUME` dimension natively — all consumed only via injection tokens — and (S1-019) the Analysis Engine's first real Consumer and first trader-visible HTTP output: a `dashboard` module (`GET /api/v1/dashboard/decision-center`, JWT-authenticated) whose `InstrumentReadingService` (the Confluence Engine Consumer) synthesizes a Dashboard-ready reading per instrument — reshaped dimension confluences, a bounded set of top contributing Providers carrying their own full Confidence taxonomy/Limitations/Traceability, honest partial-participation disclosure, and a net-direction/relevance ranking heuristic explicitly never conflated with Confidence — cached briefly (30s, asset-keyed) to avoid recomputation, and a `DashboardService` gathering the union of a trader's own Watchlist items and open Positions, ranking qualifying instruments and honestly distinguishing "no qualifying reading" from "this instrument's own computation failed" from "every instrument failed." Required one additive, backward-compatible extension to the Confluence Engine's own interface (`computeConfluenceWithEvidence()`, DEC-2026-024) so this Consumer's own Confidence/Traceability need could be met without re-invoking the Execution Engine a second time per instrument — and (S1-020) a `morning-brief` module (`GET /api/v1/morning-brief`, JWT-authenticated) whose `NarrativeComposerService` deterministically composes `DashboardService.getDecisionCenter()`'s own output (reused verbatim, zero independent aggregation) into a Morning Brief narrative: a story-before-numbers sentence per ranked instrument, a "why" quoting the lead contributor's own interpretation verbatim, a confidence explanation naming which Confidence kind is reported and quoting its own existing explanation text, an uncertainty explanation built from disclosed disagreement/non-participation/data-quality, and an explicit, calm "No Trade is the correct decision" statement when nothing qualifies, distinct in wording from both a zero-tracked-instruments case and a total-computation-failure (`DEGRADED`) case — the deterministic, template-based Narrative Composer `25_PRODUCT_BLUEPRINT.md` itself distinguishes from the separate, later, generative AI Reasoning Layer (P1, not built).
-   Web (`apps/web`) — Next.js foundation, plus (S1-021) a minimal internal Developer Preview and (S1-022) the first production Dashboard UI. `/login` (register/sign-in, proxied server-side to the existing, unmodified auth endpoints) is unchanged since S1-021. `/` is now the production Dashboard: a Server Component with a fixed reading order (Decision Readiness hero -> Morning Brief preview -> Top Instruments -> Supporting Information), still rendering only the real `GET /dashboard/decision-center` and `GET /morning-brief` output, still zero new backend logic. The hero shows the single top-ranked instrument's own story (quoted verbatim from the Narrative Composer, S1-020) plus a shared `ConfidenceDisclosure` component (`apps/web/src/components/dashboard-parts.tsx`, the DASH-003 equivalent named as a future cross-screen primitive in `26_DASHBOARD_HOME_SPECIFICATION.md` Engineering Observation 3) pairing Confidence and Uncertainty at equal visual weight, collapsed by default (progressive disclosure) and quoting the Narrative Composer's own real explanation text -- never recomputed on the frontend. Top Instruments is reduced to comparison-only columns (symbol, market, direction, agreeing-dimension count) so it never repeats the hero's own narrative/confidence text. A restrained, direction-only color system was introduced directly in `globals.css` (no design system, no theme tokens, no CSS framework) -- color is always paired with a text label, never the sole carrier of meaning. Per this Sprint's own "execution over architecture" instruction, no Sprint Brief/Decision Log entry was produced (no backend or architectural change was made); see the session record for the full UI-decision rationale and non-binding engineering observations delivered directly to Product Leadership.
-   Database (`packages/database`) — Prisma client with `User`, `Exchange`, `Market`, `Asset`, `Watchlist`, `WatchlistItem`, `FavouriteAsset`, `Portfolio`, `Position`, `Transaction`, `MarketQuote`, `Candle` models (identity, trading catalog, watchlist/favourite, portfolio/position-accounting, and market-data-cache domains; no order-execution model yet, no real external market-data vendor integrated yet, and no analytics-specific model — S1-006, S1-007, S1-008, S1-009, S1-010, S1-011, S1-012, S1-013, S1-014, S1-015, S1-016, S1-017, and S1-018 each introduced zero new Prisma models by design).
-   Shared Packages (`packages/{tooling,types,utils,validation}`).
-   Documentation (`documentation/zos`, `documentation/ai`).

## Pending Work

-   No approved Sprint Brief beyond S1-020. Per `25_PRODUCT_BLUEPRINT.md` §10, Watchlist/Portfolio annotation enrichment (steps 4-5) and Morning Brief's own full screen assembly (step 6) are the next candidate work; none has been proposed or approved. The remaining originally-planned M1 areas (further Business Services work, full authorization refinement, generic Core APIs) also remain not started, superseded in practice by the Phase-2-directed product-implementation path.
-   Non-blocking deviations accepted at S1-001 through S1-020 closure (see Known Technical Debt).

## Architecture Decisions

-   ADR-001 — JWT-Based Authentication (S1-001 Foundation). Approved 2026-07-11. See `12_ADR_INDEX.md`.
-   ADR-002 — Argon2id Password Hashing (S1-002 User Management). Approved 2026-07-12. See `12_ADR_INDEX.md`.
-   ADR-003 — Market Data Provider Abstraction (S1-005 Market Data Foundation). Approved 2026-07-12. See `12_ADR_INDEX.md`. The only registered implementation is a simulated provider — not real market data; a future real vendor requires its own superseding/additional ADR.
-   ADR-004 — Background Job Scheduling for Market Data Synchronization (S1-005). Approved 2026-07-12. See `12_ADR_INDEX.md`.
-   No new ADR was created for S1-003; catalog-mutation authorization applies the existing `Role` enum via DEC-2026-004 (see `11_DECISION_LOG.md`), which does not introduce new architecture.
-   No new ADR was created for S1-004; financial-data precision and concurrency safety apply already-approved Prisma/PostgreSQL capabilities via DEC-2026-005 (see `11_DECISION_LOG.md`), which does not introduce new architecture.
-   No new ADR was created for S1-006; the Trading Analytics layer composes already-approved services behind a new read-only module, per DEC-2026-009 (scoring/staleness calibration) and DEC-2026-010 (a bug fix, not a new decision) — see `11_DECISION_LOG.md`.
-   ADR-005 — Shared Deterministic Computation Infrastructure (Indicator Engine, Swing Detection, Regime/Context Service). Approved 2026-07-12. See `12_ADR_INDEX.md`. Implemented by S1-007 per DEC-2026-011 (computationVersion scheme, golden-dataset sourcing disclosure, calibration non-defaulting).
-   ADR-006 — Analysis Provider Plugin Architecture & Standard Output Contract. Approved 2026-07-12. See `12_ADR_INDEX.md`. Framework implemented by S1-008 per DEC-2026-012 (dependency-declaration syntax, tiering, circuit-breaker/timeout defaults, confidence labeling, computationVersion scheme). First real Provider (`WyckoffProvider`) implemented by S1-009 per DEC-2026-013 (event-detection thresholds, golden-dataset sourcing, Methodology Confidence Ceiling, and completion of the generic four-part Confidence contract). Second real Provider (`IctSmcProvider`) implemented by S1-010 per DEC-2026-014 (event-detection thresholds, golden-dataset sourcing, Methodology Confidence Ceiling of `60`, and the `DisplacementLeg` ICT-internal design) — the framework's methodology-neutrality was verified directly, not merely asserted: zero change to the generic contract, Execution Engine, Registry, Confidence Model, Traceability, Lifecycle, Observability, or Dependency Resolution was required to register a second, structurally unrelated methodology. Third real Provider (`ElliottWaveProvider`) implemented by S1-011 per DEC-2026-015 (event-detection thresholds, golden-dataset sourcing, Methodology Confidence Ceiling of `75`, and the Fibonacci-guideline reuse design) — the same zero-framework-change methodology-neutrality result held a second time, and the Indicator Engine's `fibonacciLevels()` calculator (S1-007) was consumed by a real Provider for the first time.
-   ADR-007 — Confluence Architecture: Normalization & Weighting-Readiness. Approved 2026-07-12. See `12_ADR_INDEX.md`. Implemented by S1-012 per DEC-2026-016 (per-Provider `normalize()` dimension mappings, `strength` scale calibration, top-3 attribution bound, vocabulary schema version `1.0.0`) — `EqualWeightStrategy` is the only `ConfluenceWeightStrategy` implementation this phase; methodology-family-aware, dimension-level aggregation proven false-consensus-safe by a dedicated fixture test. Extended by S1-013's `normalize()` mapping for the fourth real Provider (`HarmonicPatternsProvider`, per DEC-2026-017), S1-014's for the fifth (`ClassicalChartPatternsProvider`, per DEC-2026-018), S1-015's for the sixth (`PriceActionProvider`, per DEC-2026-019), S1-016's for the seventh (`SupplyDemandProvider`, per DEC-2026-020), S1-017's for the eighth (`FibonacciAnalysisProvider`, per DEC-2026-021), and S1-018's for the ninth (`VsaProvider`, per DEC-2026-023) — each with zero change to the Confluence Engine or vocabulary schema itself, a repeated live proof of the architecture's own extensibility claim.
-   Fourth real Provider (`HarmonicPatternsProvider`) implemented by S1-013 per DEC-2026-017 (ratio-table calibration for four named patterns, golden-dataset sourcing, Methodology Confidence Ceiling of `65`, and a `volatilityState`-based Regime-Adjusted Confidence design) — the same zero-framework-change methodology-neutrality result held a fourth time.
-   Fifth real Provider (`ClassicalChartPatternsProvider`) implemented by S1-014 per DEC-2026-018 (shape-tolerance calibration, confirmation-status scoring design, golden-dataset sourcing, Methodology Confidence Ceiling of `80`) — the same zero-framework-change methodology-neutrality result held a fifth time; `normalize()` extended with zero change to the Confluence Engine or vocabulary schema, a second live proof of ADR-007's own extensibility claim.
-   Sixth real Provider (`PriceActionProvider`) implemented by S1-015 per DEC-2026-019 (deterministic five-state reaction classification over a single key level, quality/momentum/continuation-exhaustion scoring, a boundary-proximity bounded-hypothesis mechanism distinct from every prior Provider's own bounded multi-window search, a `volatilityState`-bifurcated-by-reading-type Regime-Adjusted Confidence rule, Methodology Confidence Ceiling of `70`, and the first genuinely native `normalize()` `MOMENTUM` population) — the same zero-framework-change methodology-neutrality result held a sixth time; `normalize()` extended with zero change to the Confluence Engine or vocabulary schema, a third live proof of ADR-007's own extensibility claim.
-   Seventh real Provider (`SupplyDemandProvider`) implemented by S1-016 per DEC-2026-020 (bounded base-and-departure zone detection, independently-tracked freshness/mitigation health decay, a one-per-side bounded-hypothesis mechanism distinct from every prior Provider's own, a `trendDirection`-bifurcated-by-zone-type Regime-Adjusted Confidence rule — the first to use that axis — Methodology Confidence Ceiling of `68`, and the first Provider not to inject `SWING_DETECTOR` directly) — the same zero-framework-change methodology-neutrality result held a seventh time; `normalize()` extended with zero change to the Confluence Engine or vocabulary schema, a fourth live proof of ADR-007's own extensibility claim, including a legitimate, deliberate `LIQUIDITY`-dimension-sharing with `IctSmcProvider`'s own use of it.
-   Eighth real Provider (`FibonacciAnalysisProvider`) implemented by S1-017 per DEC-2026-021 (bounded multi-leg retracement/extension level generation over at most the four most recent swing legs, cross-leg confluence clustering, untested/respected/broken reaction classification, a proximity-ranked bounded-hypothesis mechanism distinct from every prior Provider's own — the first to bound by nearest-to-current-price rather than score or type-sidedness — a `volatilityState`-bifurcated-by-level-type Regime-Adjusted Confidence rule, and Methodology Confidence Ceiling of `72`) — the same zero-framework-change methodology-neutrality result held an eighth time; `normalize()` extended with zero change to the Confluence Engine or vocabulary schema, a fifth live proof of ADR-007's own extensibility claim, despite being the second Provider (after `ElliottWaveProvider`) to directly consume the Indicator Engine's `fibonacciLevels()` calculator and sharing conceptual ratio vocabulary with `HarmonicPatternsProvider`.
-   Ninth real Provider (`VsaProvider`) implemented by S1-018 per DEC-2026-023 (per-bar spread/volume/close-position classification, a disclosed priority-ordered five-signal detection mechanism proven mutually exclusive by construction, a recency-ranked bounded-hypothesis mechanism distinct from every prior Provider's own, a signal-category-bifurcated (climax-type vs. quiet-type) Regime-Adjusted Confidence rule, and Methodology Confidence Ceiling of `74`) — the same zero-framework-change methodology-neutrality result held a ninth time; `normalize()` extended with zero change to the Confluence Engine or vocabulary schema, a sixth live proof of ADR-007's own extensibility claim, and the first Provider to populate the `VOLUME` dimension natively. Preceded by DEC-2026-022, a dedicated pre-implementation architecture prerequisite check establishing that no `dependsOn` on `WyckoffProvider` is required, reversing only the anticipatory dependency framing recorded at S1-009/ADR-006 (the Execution Engine's own `dependsOn` mechanism remains genuinely unexercised by any registered Provider).
-   ADR-007 extended a seventh time by S1-019's `computeConfluenceWithEvidence()` (DEC-2026-024) — the Confluence Engine's own first additive interface extension since S1-012, adding a method that returns each participating Provider's own complete `AnalysisProviderResult` alongside the existing `ConfluenceResult`, from one shared execution run (verified by a dedicated test asserting the Execution Engine is invoked exactly once regardless of which public method is called). `computeConfluence()`'s own existing contract, behavior, and every existing caller/test are unchanged. This is the Analysis Engine's first real Consumer-driven architecture change — anticipated by `22_ANALYSIS_ENGINE_ARCHITECTURE.md`'s own Trace Store discussion ("reachable by a Consumer, e.g. displayed on a Dashboard"), not a speculative framework addition.
-   No new ADR was created for S1-020; the Narrative Composer is a deterministic templating layer over S1-019's own already-approved output, introducing no new technology or mechanism, per DEC-2026-025 (instrument-scope reconciliation and template calibration) — see `11_DECISION_LOG.md`.

## Known Technical Debt

-   Formal per-dependency `14_DEPENDENCY_POLICY.md` review was not run individually for S1-001/S1-002's supporting libraries (e.g. `nestjs-pino`, `@nestjs/passport`, `argon2`). Owner: Architecture Team. Planned resolution: address in a future dependency audit; accepted as non-blocking at S1-001 and S1-002 closure.
-   Automated test coverage now includes health, auth, user-registration/login, trading catalog, watchlist/favourite, portfolio/position, market-data, and analytics flows, but `apps/web` still has no automated tests. Owner: next implementation sprint. Planned resolution: expand alongside future feature work.
-   Extended user profile fields (for personalized trading data, performance analysis, risk insights) and compliance/KYC/session-policy considerations were intentionally deferred at S1-002 — see `documentation/zos/sprints/S1-002_SPRINT_BRIEF.md`, Missing Decisions section.
-   No API exists to create or promote an `ADMIN` user; S1-003 intentionally deferred this as a user-management concern, not a trading-catalog concern — see `documentation/zos/sprints/S1-003_SPRINT_BRIEF.md`, Missing Decisions section.
-   Catalog data seeding/ingestion strategy (how real-world exchange/market/asset data is sourced and kept current at scale) was intentionally deferred at S1-003 — see the same Missing Decisions section.
-   Unrealized P/L display and its live market-data source were intentionally deferred at S1-004 — this sprint exposes only `quantity`/`averageCost`/computed `costBasis`, no price feed. Multi-currency portfolio support was also intentionally deferred. See `documentation/zos/sprints/S1-004_SPRINT_BRIEF.md`, Missing Decisions section. (Unrealized P/L itself was subsequently delivered as part of S1-006's Trading Analytics layer.)
-   **All market data (quotes and candles) is simulated, not real, as of S1-005** — no external market-data vendor has been reviewed or approved by the Architecture Team. Owner: Architecture Team. Planned resolution: a future ADR selecting and integrating a real vendor behind the existing `MarketDataProvider` interface, requiring no change to caching, rate limiting, retry, background sync, or the HTTP API. See `documentation/zos/sprints/S1-005_SPRINT_BRIEF.md`, Missing Decisions section, and ADR-003. This limitation is inherited unchanged by S1-006's analytics, which are only as real as the underlying simulated quotes.
-   Unrealized P/L display, intraday/multi-timeframe candles, and multi-currency quotes were intentionally deferred at S1-005 — see the same Missing Decisions section.
-   Portfolio/performance history, charting, alerts, a Risk Engine, a Decision Engine, and AI-driven insight were intentionally deferred at S1-006 — this sprint is stateless and point-in-time by design, computing analytics live with no persisted history. See `documentation/zos/sprints/S1-006_SPRINT_BRIEF.md`, Missing Decisions section.
-   S1-007 introduces no trader-visible output by design — the Indicator Engine, Swing Detection Infrastructure, and Regime/Context Service are internal, composable-only services with no HTTP endpoint, consumed by no code yet. They remain unused until a real Analysis Provider (S1-009 onward) begins consuming them. Swing Detection sensitivity and Regime/Context Service threshold calibration have no code-level default and must be supplied by each caller — see `documentation/zos/sprints/S1-007_SPRINT_BRIEF.md`, Missing Decisions section, and DEC-2026-011.
-   S1-008's Analysis Provider Framework registers zero real Providers by design — the registry, Execution Engine, circuit breaker, and Lifecycle gating are exercised only by in-test-only fixture Providers. `normalize()`'s placeholder (`: void`) was completed by S1-012 per ADR-007's vocabulary (see below); `traceability` is in-memory only, with no persistence layer. Finding B (whether a `DEPRECATED` Provider's `computationVersion` may still increment) remains an open question that must be resolved before any Provider is actually deprecated — see `22_ANALYSIS_ENGINE_ARCHITECTURE.md`, Additional Findings (Finding B), and `documentation/zos/sprints/S1-008_SPRINT_BRIEF.md`. Not affected by S1-009 (`WyckoffProvider` is `ACTIVE` only; no Lifecycle transition occurs).
-   S1-009's `WyckoffProvider` is scoped to the single canonical Wyckoff Schematic #1 (not Schematic #2, re-accumulation/re-distribution, or Point & Figure counting — the last excluded as a scope/complexity decision, not an architectural blocker, since P&F does not require intraday data). VSA (Volume Spread Analysis), already declared as depending on Wyckoff's active-range output, is not built; how a future VSA Provider will read that output beyond the generic `AnalysisProviderResult` contract is unsolved, deferred to whichever sprint builds VSA. In a sparse series with exactly two swing highs positioned precisely at the Automatic Rally and Sign-of-Strength events (no intervening minor high), the range's resistance boundary equals the Sign-of-Strength's own price, making the breakout undetectable — a disclosed V1 edge case, not expected in real multi-week data. See `documentation/zos/sprints/S1-009_SPRINT_BRIEF.md`, Missing Decisions section, and DEC-2026-013.
-   S1-010's `IctSmcProvider` is scoped to a bounded V1 toolkit (Order Blocks, Fair Value Gaps, Liquidity Sweeps) — Optimal Trade Entry/premium-discount Fibonacci zones, Power of Three, Breaker/Mitigation/Rejection Blocks, and other Order Block variants are deferred as scope/complexity decisions, candidates for a future extension of this same Provider. Killzones and any time-of-day/session-window concept are **architecturally blocked, not merely deferred**: `MarketSeries` carries daily-bar points with no intraday timestamp granularity or session metadata. The `DisplacementLeg` internal concept (per Architecture Team Implementation Guidance #1) is confined to `providers/ict-smc/` and must not be promoted into the generic framework without a future ADR, even if a later Provider appears to need something similar. See `documentation/zos/sprints/S1-010_SPRINT_BRIEF.md`, Missing Decisions section, and DEC-2026-014.
-   S1-011's `ElliottWaveProvider` is scoped to the single canonical 5-wave motive (impulse) count only — corrective wave counting (A-B-C zigzag/flat/triangle), diagonal triangles (and Rule 3's documented exception for them), Wave Personality/Alternation/Equality, and multi-degree wave labeling are all deferred as scope/complexity decisions, candidates for a future extension of this same Provider. `ElliottWaveProvider`'s own internal types (`WaveCountCandidate` and related) are confined to `providers/elliott-wave/` and must not be promoted into the generic framework without a future ADR, per the Architecture Team's explicit direction, even if a later Provider appears to need something similar. See `documentation/zos/sprints/S1-011_SPRINT_BRIEF.md`, Missing Decisions section, and DEC-2026-015.
-   S1-012's Confluence Engine implements only `EqualWeightStrategy` — real differential (data-driven) weighting is intentionally deferred until historical validation data exists, and requires its own superseding/additional Decision Log entry (the `ConfluenceWeightStrategy` interface exists precisely so this needs no Provider or Confluence Engine contract change). The "expected vs. unexpected disagreement" Methodology Conflict Matrix classification (`22_ANALYSIS_ENGINE_ARCHITECTURE.md`'s own Additional Findings) is also deferred — V1 reports raw, correctly-attributed disagreement only, since no Provider yet self-declares a conflict-expectation tag. Methodology-family-aware grouping is proven correct only via a dedicated fixture test — no two of the nine currently-registered Providers share a `methodologyFamily` (S1-013's `HarmonicPatternsProvider`, S1-014's `ClassicalChartPatternsProvider`, S1-015's `PriceActionProvider`, S1-016's `SupplyDemandProvider`, S1-017's `FibonacciAnalysisProvider`, and S1-018's `VsaProvider` each register their own distinct family), so this mechanism remains unexercised by real Provider data until two Providers sharing a family are registered. Trace Store persistence remains deferred — Confluence is still internal/composable only, computed live per request, with full per-Provider traceability referenced by ID (recoverable from the same request/response's own `AnalysisProviderResult[]`), never embedded. See `documentation/zos/sprints/S1-012_SPRINT_BRIEF.md`, Missing Decisions section, and DEC-2026-016.
-   S1-013's `HarmonicPatternsProvider` is scoped to a bounded V1 toolkit of four named patterns (Gartley, Bat, Butterfly, Crab) — Shark, Cypher, Deep Crab, 5-0, Alternate Bat, and Three Drives are deferred as scope/complexity decisions, candidates for a future extension of this same Provider. PRZ width computation and trade-entry/stop/target price levels are correctly out of scope for an Analysis Provider (a trading-recommendation concern). With the honestly-sourced ratio tables, the four patterns' own `AD` bands are mutually disjoint, so genuine simultaneous multi-pattern ambiguity is rare through realistic price action alone — the underlying "check every table independently, never merge" mechanism is proven correct via a dedicated synthetic-fixture unit test (`matchPatternTypes()`'s own disclosed `tables` parameter), not by incidental behavior on today's four patterns' real data, the same category of proof S1-012's family-grouping mechanism required. See `documentation/zos/sprints/S1-013_SPRINT_BRIEF.md`, Missing Decisions section, and DEC-2026-017.
-   S1-014's `ClassicalChartPatternsProvider` is scoped to a bounded V1 toolkit of two pattern families (Head and Shoulders + Inverse, Double Top/Bottom) — Triangles, Flags, Pennants, Wedges, Rectangles, Rounding Tops/Bottoms, and Cup-and-Handle are deferred as scope/complexity decisions, candidates for a future extension of this same Provider. Price-target projection (the classic Edwards & Magee "measured move") is correctly out of scope for an Analysis Provider (a trading-recommendation concern). A genuine structural property, not a defect: a Head and Shoulders candidate's own neckline (its two Troughs) structurally also satisfies a Double-Bottom-shape check on those same three points (neckline-levelness and trough-symmetry are the identical computation), so real overlap between the two pattern families is common, not rare — the mirror-image finding to Harmonic Patterns' own S1-013 discovery that its four patterns' ratio bands are mutually disjoint. Both patterns are honestly disclosed as independent, never-merged hypotheses, per this system's own bounded multi-hypothesis design. See `documentation/zos/sprints/S1-014_SPRINT_BRIEF.md`, Missing Decisions section, and DEC-2026-018.
-   S1-015's `PriceActionProvider` is scoped to a single, most-recent key level (Non-Scope: historical multi-level scanning, deferred) and deliberately excludes named candlestick-pattern recognition (a permanent design boundary, not deferred scope) and volume-based analysis (deliberately excluded to preserve independence from Wyckoff's own volume-climax methodology). Trend lines/channels are deferred; multi-timeframe analysis is architecturally blocked, mirroring ICT/SMC's own Killzones exclusion. Two genuine wording clarifications between the Sprint Brief's own illustrative language and the delivered implementation were disclosed via the Decision Log rather than left silent: the wick-to-range ratio "threshold" is realized as this reading's own Detection Confidence measurement rather than a sixth classification state (the five states must remain exhaustive with no fallback category), and momentum is anchored to the breakout bar itself (not the key level) and expressed as pure ATR-relative displacement rather than a per-bar rate. See `documentation/zos/sprints/S1-015_SPRINT_BRIEF.md`, Missing Decisions section, and DEC-2026-019.
-   S1-016's `SupplyDemandProvider` deliberately excludes volume-based analysis (Wyckoff-independence) and momentum/velocity scoring (Price-Action-independence) as permanent design boundaries, and deliberately never reuses `IctSmcProvider`'s own displacement/Order-Block detection despite a shared retail-trading lineage — verified both by a mechanical Independence Boundary Test and a fully self-contained implementation. Trend lines/channels and cross-window zone-merging heuristics are deferred; multi-timeframe analysis is architecturally blocked, the same limitation already disclosed for `IctSmcProvider`'s own Killzones exclusion. A genuine, non-obvious implementation property, not a defect: `UNMITIGATED` only ever co-occurs with `FRESH` in the delivered health-tracking algorithm (any touch at all upgrades mitigation to at least `PARTIALLY_MITIGATED`), so only five of nine combinatorially-possible freshness/mitigation pairs are ever reachable — disclosed in the Decision Log rather than left as an unexplained gap. See `documentation/zos/sprints/S1-016_SPRINT_BRIEF.md`, Missing Decisions section, and DEC-2026-020.
-   S1-017's `FibonacciAnalysisProvider` is scoped to retracement/extension levels drawn from at most the four most recent swing legs (`MAX_SWINGS_FOR_LEGS`) — Fibonacci time zones, circles, arcs, and fans are deferred as scope/complexity decisions, candidates for a future extension of this same Provider. Cross-window zone-merging (matching the same deferral already disclosed for `SupplyDemandProvider`) is deferred; multi-timeframe analysis is architecturally blocked, the same limitation already disclosed for `IctSmcProvider`'s own Killzones exclusion. A genuine correctness fix, not a silent gap: the first implementation draft determined a candidate's own support/resistance role from the series' final close — the same value used for proximity ranking — which made a `BROKEN` reaction classification structurally unreachable whenever only one subsequent point existed; fixed by determining role from the *first* subsequent point's own close instead, while proximity ranking continues to use the true final close, disclosed in the Decision Log rather than left as an unexplained gap. See `documentation/zos/sprints/S1-017_SPRINT_BRIEF.md`, Missing Decisions section, and DEC-2026-021.
-   S1-018's `VsaProvider` is scoped to five named signal types (No Demand, No Supply, Upthrust, Shakeout, Stopping Volume), each detected at the single-bar level only — no trading-range/schematic-phase concept is read or required, the deliberate boundary this Provider's own Sprint claimed from `WyckoffProvider`'s own S1-009-disclosed deferral. Multi-timeframe analysis is architecturally blocked, the same limitation already disclosed for `IctSmcProvider`'s own Killzones exclusion. A dedicated pre-implementation architecture prerequisite check (DEC-2026-022) established that this Provider requires no `dependsOn` on any other Provider, correcting the anticipatory "VSA depends on Wyckoff" framing recorded at S1-009/ADR-006 by reading the Execution Engine's own source directly rather than assuming it still held; the Execution Engine's `dependsOn` mechanism itself remains genuinely unexercised by any registered Provider, an open question deferred until a future Provider actually needs a Provider-to-Provider data channel. Two disclosed self-review fixes, not defects: doc-comment wording in this Provider's own source twice tripped its Independence Boundary Test by naming other Providers while disclosing legitimate shared-terminology/axis-reuse relationships — reworded in both cases, the same recurring false-positive class caught at S1-007/S1-009/S1-010/S1-011/S1-016. See `documentation/zos/sprints/S1-018_SPRINT_BRIEF.md`, Missing Decisions section, and DEC-2026-022/DEC-2026-023.
-   S1-019's Dashboard backend has no real-time update mechanism (poll or push) — every `26_DASHBOARD_HOME_SPECIFICATION.md` component's own Update Behaviour remains `TODO`; `GET /dashboard/decision-center` is a plain request/response endpoint only. No Watchlist/Portfolio annotation endpoint exists yet (no screen specification for either area has been written). The `InstrumentReading` cache is shared across traders tracking the same instrument (asset-keyed, not trader-keyed) — verified intentional and safe (the cached value carries no trader-specific data), not a defect. A design gap was found and corrected during implementation, before any test was written against the flawed shape: the ranking utility initially had no access to per-dimension contributor detail once `InstrumentReading.dimensions` was reshaped for the public response; resolved by computing `netDirection`/`relevanceScore` once, inside `InstrumentReadingService`, from the raw (pre-reshape) Confluence dimensions. See `documentation/zos/sprints/S1-019_SPRINT_BRIEF.md`, Missing Decisions section, and DEC-2026-024.
-   S1-020's Morning Brief inherits every limitation already disclosed for S1-019 (no real-time update mechanism, no Watchlist/Portfolio annotation endpoint) since it composes `DashboardService.getDecisionCenter()` directly. Morning Brief's own instrument scope was reconciled to mirror Dashboard's (Watchlist ∪ open Positions) rather than the Blueprint's own originally-narrower "Watchlist only" framing, a disclosed refinement made to avoid duplicating `DashboardService`'s own orchestration — see DEC-2026-025. Calendar/News and Trading Journal continuity remain unbuilt, optional inputs per `25_PRODUCT_BLUEPRINT.md` §2 (Morning Brief), not included. No generative AI Reasoning Layer is used or built — Morning Brief V1 is deterministic template composition only. See `documentation/zos/sprints/S1-020_SPRINT_BRIEF.md`, Missing Decisions section, and DEC-2026-025.

-   **Analysis Provider Architecture Phase Closure (S1-007→S1-018).** Formally closed 2026-07-14 — see `documentation/zos/23_ANALYSIS_PROVIDER_PHASE_COMPLETION.md` for the full Architecture Review, extensibility validation, consolidated known-limitations disclosure, and readiness assessment. Nine Providers registered, zero shared code between any pair (mechanically verified), zero framework or Confluence Engine change required across the six Providers added after S1-012, zero new ADR required across the entire phase. No new Provider or Sprint was authorized as part of this closure; a non-binding recommendation (a Confluence Engine Consumer, e.g. a read-only Dashboard API) was offered for the Architecture Team's own consideration, not implemented.
-   **S1-019's `InstrumentReadingService`/`DashboardService`** are scoped to exactly `GET /dashboard/decision-center` (DASH-002) — no Watchlist/Portfolio annotation endpoint exists yet (no screen specification for either has been written), though `InstrumentReadingService` is exported from `DashboardModule` specifically so a future one can reuse it without modification. Default historical lookback (180 days), bounded top-contributor count (5), the Decision-Relevance ranking formula, and cache TTL (30s) are all implementation-time calibration, disclosed in DEC-2026-024, not architecture. A trader tracking zero instruments reports `NO_CLEAR_OPPORTUNITY`, not a distinct state — `26_DASHBOARD_HOME_SPECIFICATION.md`'s own DASH-002 Empty State did not explicitly address this edge case; it is treated as a degenerate case of "no qualifying reading," disclosed here rather than left as an unstated assumption.

## Governance Documents

-   **Zenith Product Constitution v1.0** — Approved 2026-07-14. `documentation/zos/24_ZENITH_PRODUCT_CONSTITUTION.md` (ZOS-024). The highest project authority on product philosophy, trader psychology, design, and AI persona — peer, not subordinate or superior, to `06_PROJECT_CONSTITUTION.md`'s own engineering-process authority (see the Constitution's own §0.1). Establishes: Vision/Mission for the trading product itself (§1-§2, distinct from `02_PRODUCT_VISION.md`'s engineering-project vision); Product DNA (§3) — Zenith's immutable identity as an explainable trading-intelligence platform that never issues BUY/SELL recommendations, its five Non-Negotiable Principles, what it will never become, its feature-acceptance gate, and its identity-preservation rule; Core Product/Design Philosophy (§4-§5); Trader Psychology, Human Behavior Principles, and Visual Psychology (§6-§8), each claim tagged Established Evidence / Industry Best Practice / Zenith-Specific Decision; User Journey Philosophy (§9); Screen Philosophy (§10) with the nine currently-approved product areas (Dashboard/Home, Morning Brief, Trading Journal, Watchlist, Portfolio, Alerts, Calendar/News, COT & Reports, AI Workspace) stated as a Purpose/Psychological-Objective/Business-Objective/Success-Criteria framework only — no UI design, and expansion requires its own leadership approval; AI Personality (§11) for Zenith's own embedded product AI Assistant, distinct from the Implementation Engineer's own persona in `ZENITH_AI_SYSTEM_PROMPT.md`; Decision Philosophy (§12) documenting the reasoning behind ten permanent Product Rules (§13, including "story before data," "decision before chart," "no-trade is a valid outcome," and "explain confidence"/"explain uncertainty"); a Design Constitution (§14) and Implementation Constitution (§15) binding on every future screen and engineer; and a Governance section (§16) requiring Architecture Team/leadership approval and a Decision Log entry for any amendment. This document authorizes no implementation on its own — every future screen still requires its own approved Sprint Brief that also complies with this Constitution.
-   **Zenith Product Blueprint v1.0** — Approved 2026-07-14. `documentation/zos/25_PRODUCT_BLUEPRINT.md` (ZOS-025). Converts the frozen Product Constitution into an implementation execution map, treating the Constitution as immutable throughout. Defines, for each of the nine approved product areas only, its Purpose/Inputs/Outputs/Dependencies/Priority/Entry Points/Exit Points (no UI); a six-phase User Flow (Morning/Analysis/Decision/Monitoring/Review/Learning, mapping Constitution §9's own six phases onto the concrete product surface); a hub-and-spoke Navigation Blueprint (Dashboard as root); an Information Flow pipeline from Market Data through the already-built Analysis Providers and Confluence Engine into two net-new components (a Confluence Engine Consumer and a deterministic Narrative Composer) and onward to Morning Brief, Dashboard, Trading Journal, and Learning; an AI Interaction Flow distinct from that deterministic pipeline; a Feature Dependency Map identifying the Confluence Engine Consumer as the single most-depended-upon net-new component; an MVP Definition scoping Version 1 to exactly four areas (Dashboard, Morning Brief, Watchlist, Portfolio) built on that Consumer plus the Narrative Composer and already-existing backend services; an explicit Out-of-Scope list; and a recommended eleven-step Implementation Order. No UI, code, or Sprint Brief was produced or authorized by this document. Implementation Order step 1 (the Confluence Engine Consumer) was completed by S1-019; step 2 (the Narrative Composer) was completed by S1-020.
-   **Dashboard (Home) Experience Specification** — Proposed 2026-07-14, Product Leadership review complete. `documentation/zos/26_DASHBOARD_HOME_SPECIFICATION.md` (ZOS-026). The Golden Reference Sprint specification translating the Constitution and Blueprint into an 11-component Dashboard architecture (`DASH-001` through `DASH-011`), each specified across Purpose/Priority/Inputs/Outputs/Dependencies/Interaction/States/Update Behaviour/Accessibility/Performance/Future Extensibility. S1-019 implements the backend `DASH-002` (Decision Readiness Summary) depends on.
-   **Zenith Experience Language v1.0** — Proposed 2026-07-14. `documentation/zos/27_ZENITH_EXPERIENCE_LANGUAGE.md` (ZOS-027). The experience-layer document sitting between the Product Constitution (why) and implementation specifications (what/how built) — Attention Hierarchy, Decision Flow, Cognitive Load Rules, Calm Interface Principles, Visual Weight Philosophy, Trust Building, Decision Psychology, Language Philosophy, Component Psychology, and 15 Experience Principles. No visual design; governs how future screens should feel, not look.

## Known Risks

-   None currently acknowledged beyond the Known Technical Debt items above.

## Blockers

-   None currently active.

## Open Questions

-   **What is the next Sprint Brief, now that S1-019/S1-020 (Implementation Order steps 1-2) are closed?** Watchlist/Portfolio annotation enrichment (steps 4-5, reusing `InstrumentReadingService`) and Morning Brief's own full screen assembly (step 6) are the Blueprint's own next-indicated candidates (`25_PRODUCT_BLUEPRINT.md` §10), but none has been proposed or approved — this remains a fresh Architecture Team decision, bound by the same Feature Acceptance Philosophy (§3.4)/Screen Philosophy framework (§10.1)/dependency ordering (§7, §10) discipline S1-019/S1-020 were themselves held to.
-   **Expansion of the approved product surface** (`24_ZENITH_PRODUCT_CONSTITUTION.md` §10.2's nine areas: Dashboard/Home, Morning Brief, Trading Journal, Watchlist, Portfolio, Alerts, Calendar/News, COT & Reports, AI Workspace) requires its own leadership approval per §10.3 — none is proposed or approved at this time.
-   **Vendor selection for Calendar/News and COT & Reports** (`25_PRODUCT_BLUEPRINT.md` §2, §9) is blocked on its own future ADR, the same category of open decision already disclosed for real market data (ADR-003) — no vendor has been reviewed or approved for either.

# Update Policy

Project Brain may be updated only after:

-   Sprint approval
-   Architecture review
-   Milestone review
-   Approved architectural decisions

Implementation engineers must never modify this document directly.

# Related Documents

-   07_ENGINEERING_WORKFLOW.md
-   08_ROADMAP.md
-   10_AI_ENGINEER_GUIDE.md
-   20_AI_BOOT_SEQUENCE.md
