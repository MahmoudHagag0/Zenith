{
  "name": "@zenith/design-tokens",
  "version": "0.1.0",
  "private": true,
  "description": "Zenith Design Tokens -- production token delivery for D2-001 through D2-004 (M5-001). Single TypeScript source of truth; generates CSS custom properties for both themes.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist", "css"],
  "scripts": {
    "build": "tsc -p tsconfig.json && node dist/generate-css.js",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@zenith/tooling": "workspace:*",
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  }
}
