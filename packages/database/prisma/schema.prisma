generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// No domain models yet — out of scope for S1-001 (see Sprint Brief Non-Scope).
