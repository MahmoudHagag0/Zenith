# eslint-config-next's legacy config bridges through @rushstack/eslint-patch,
# which resolves @eslint/eslintrc assuming a hoisted node_modules tree.
# Hoist only this one package rather than disabling pnpm's strict isolation.
public-hoist-pattern[]=@eslint/eslintrc
